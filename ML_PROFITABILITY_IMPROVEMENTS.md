# Улучшения ML стратегии для прибыльности

**Дата:** 2026-01-29  
**Цель:** Сделать ML стратегию прибыльной через улучшение таргета, весов классов и символ-специфичных настроек

---

## ✅ ВЫПОЛНЕНО

### 1. Улучшен таргет для обучения (`bot/ml/feature_engineering.py`)

**Новые параметры:**
- `min_profit_pct: float = 1.5` - Минимальная прибыль 1.5% для классификации как LONG/SHORT
- `max_hold_periods: int = 32` - Максимум 32 * 15m = 8 часов для качественных сделок

**Улучшения:**
- ✅ Фильтрация по реальному времени достижения TP (не просто forward_periods)
- ✅ Строгая проверка RR >= 1.8-2.0
- ✅ Фильтрация медленных сделок (> 8 часов)
- ✅ Требование минимальной прибыли 1.5%

**Результат:** Модели теперь обучаются только на **качественных** сделках с хорошим RR и быстрым достижением цели.

### 2. Улучшены веса классов во всех обучающих скриптах

**Новые веса:**
- **HOLD:** `base_weight * 0.1` (было 0.3) - **сильно уменьшен**
- **LONG/SHORT:** `base_weight * 3.0-4.0` (было 2.0) - **увеличен**

**Обновленные скрипты:**
- ✅ `retrain_ml_optimized.py` - веса: HOLD=0.1x, LONG/SHORT=3.0x
- ✅ `train_quad_ensemble.py` - веса: HOLD=0.1x, LONG/SHORT=3.0x
- ✅ `retrain_ultra_aggressive.py` - веса: HOLD=0.05x, LONG/SHORT=4.0x

**Результат:** Модели фокусируются на **прибыльных** LONG/SHORT сигналах, игнорируя HOLD.

### 3. Обновлены все обучающие скрипты

Все скрипты теперь используют новые параметры таргета:
- ✅ `retrain_ml_optimized.py`
- ✅ `train_quad_ensemble.py`
- ✅ `bot/ml/train_ml_model.py`
- ✅ `bot/ml/retrain_model.py`
- ✅ `retrain_ultra_aggressive.py`
- ✅ `train_lightgbm_model.py`
- ✅ `train_lstm_model.py`

---

## 📋 РЕКОМЕНДАЦИИ ПО СИМВОЛАМ

### BTCUSDT (стабильный, низкая волатильность)

**Рекомендуемые настройки:**
```python
ml_confidence_threshold: 0.70-0.75  # Высокий порог уверенности
ml_min_signal_strength: "среднее" или "сильное"  # Только сильные сигналы
ml_stability_filter: True  # Включен
ml_model_type: "triple_ensemble" или "quad_ensemble"  # Ансамбли лучше
ml_mtf_enabled: False  # Пока используем 15m (MTF хуже по тестам)
```

**TP/SL:**
- TP: 18% от маржи (1.8% от цены)
- SL: 10% от маржи (1.0% от цены)
- RR: 1.8:1

### ETHUSDT (средняя волатильность)

**Рекомендуемые настройки:**
```python
ml_confidence_threshold: 0.65-0.70  # Средний порог
ml_min_signal_strength: "умеренное" или "среднее"
ml_stability_filter: True
ml_model_type: "triple_ensemble"
ml_mtf_enabled: True  # MTF может работать лучше для ETH
```

**TP/SL:**
- TP: 18% от маржи (1.8% от цены)
- SL: 10% от маржи (1.0% от цены)
- RR: 1.8:1

### SOLUSDT (высокая волатильность)

**Рекомендуемые настройки:**
```python
ml_confidence_threshold: 0.60-0.65  # Более мягкий порог
ml_min_signal_strength: "слабое" или "умеренное"
ml_stability_filter: True
ml_model_type: "quad_ensemble"  # Больше моделей для волатильности
ml_mtf_enabled: False  # 15m лучше для волатильных
```

**TP/SL:**
- TP: 20% от маржи (2.0% от цены) - можно увеличить для волатильных
- SL: 10% от маржи (1.0% от цены)
- RR: 2.0:1

---

## 🚀 СЛЕДУЮЩИЕ ШАГИ

### 1. Переобучить модели с новыми параметрами

```bash
# Для BTCUSDT (15m, triple/quad ensemble)
python retrain_ml_optimized.py --symbol BTCUSDT

# Для ETHUSDT (MTF, triple ensemble)
python retrain_ml_optimized.py --symbol ETHUSDT

# Для SOLUSDT (15m, quad ensemble)
python retrain_ml_optimized.py --symbol SOLUSDT
```

### 2. Протестировать новые модели

```bash
python compare_ml_models.py --days 30 --symbols BTCUSDT,ETHUSDT,SOLUSDT
```

### 3. Ожидаемые улучшения

После переобучения с новыми параметрами ожидаем:
- ✅ **WinRate:** 40-60% (было 23%)
- ✅ **Profit Factor:** > 1.2 (было 0.62)
- ✅ **Количество сделок:** 30-100 за 30 дней (было 13)
- ✅ **Средняя длительность:** < 20 часов (было 18.6h)

### 4. Настроить символ-специфичные параметры в админке

После тестирования обновить `symbol_strategy_settings.json` с рекомендуемыми значениями для каждого символа.

---

## 📊 МЕТРИКИ ДЛЯ ОЦЕНКИ

После переобучения проверяем:
1. **WinRate** - должен быть > 40%
2. **Profit Factor** - должен быть > 1.0
3. **Sharpe Ratio** - должен быть > 0.5
4. **Max Drawdown** - должен быть < 10%
5. **Количество сделок** - должно быть 30-100 за 30 дней
6. **Баланс LONG/SHORT** - должны быть оба типа сигналов

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Улучшенный таргет

**Старая логика:**
- Просто проверка изменения цены на `forward_periods` вперед
- Нет фильтрации по времени достижения цели
- Нет строгой проверки RR

**Новая логика:**
- ✅ Поиск **реального времени** достижения TP (не просто forward_periods)
- ✅ Фильтрация сделок, которые достигли цели **быстро** (< 8 часов)
- ✅ Строгая проверка RR >= 1.8-2.0
- ✅ Требование минимальной прибыли 1.5%

### Улучшенные веса классов

**Старые веса:**
- HOLD: `base_weight * 0.3`
- LONG/SHORT: `base_weight * 2.0`

**Новые веса:**
- HOLD: `base_weight * 0.1` (уменьшено в 3 раза)
- LONG/SHORT: `base_weight * 3.0-4.0` (увеличено в 1.5-2 раза)

**Результат:** Модели теперь **фокусируются** на прибыльных LONG/SHORT сигналах, игнорируя HOLD.
