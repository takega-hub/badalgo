# 🚀 Быстрый старт тестирования многопарной торговли

## Шаг 1: Проверка конфигурации

Запустите проверку конфигурации:
```bash
python check_config.py
```

Этот скрипт проверит:
- ✅ Наличие необходимых настроек
- ✅ Настройки символов
- ✅ ML модели
- ✅ Загрузку конфигурации
- ✅ MultiSymbolManager

---

## Шаг 2: Настройка .env (если еще не настроено)

Создайте `.env` файл на основе `env.example`:

```env
# Bybit API Settings (ОБЯЗАТЕЛЬНО заполнить!)
BYBIT_API_KEY=ваш_api_ключ
BYBIT_API_SECRET=ваш_api_секрет
BYBIT_BASE_URL=https://api-testnet.bybit.com  # Для тестнета

# Admin Panel Authentication
ADMIN_USERNAME=admin
ADMIN_PASSWORD=ваш_пароль
FLASK_SECRET_KEY=секретный_ключ

# Multi-Symbol Trading (НОВОЕ!)
ACTIVE_SYMBOLS=BTCUSDT,ETHUSDT,SOLUSDT
PRIMARY_SYMBOL=BTCUSDT

# Остальные настройки...
```

**Важно:**
- Для тестирования используйте **testnet** (`BYBIT_BASE_URL=https://api-testnet.bybit.com`)
- Для продакшена используйте **production** (`BYBIT_BASE_URL=https://api.bybit.com`)

---

## Шаг 3: Запуск веб-сервера

### Вариант 1: Автоматический запуск с проверками
```bash
python start_testing.py
```

### Вариант 2: Через main.py
```bash
python main.py --mode web
```

### Вариант 3: Напрямую через Flask
```bash
python -m bot.web.app
```

### Вариант 4: Через модуль (рекомендуется для продакшена)
```bash
# Используя waitress (кроссплатформенный)
waitress-serve --host=127.0.0.1 --port=5000 bot.web.app:app

# Или используя gunicorn (только Linux/Mac)
gunicorn -w 4 -b 127.0.0.1:5000 'bot.web.app:app'
```

---

## Шаг 4: Открытие веб-интерфейса

1. Откройте браузер и перейдите на:
   ```
   http://localhost:5000
   ```

2. Войдите в систему:
   - **Username:** значение из `ADMIN_USERNAME` (по умолчанию `admin`)
   - **Password:** значение из `ADMIN_PASSWORD`

---

## Шаг 5: Тестирование основных функций

### 5.1. Проверка вкладки "Symbols"

1. Откройте вкладку **"Symbols"**
2. Проверьте:
   - ✅ Отображаются все доступные символы (BTCUSDT, ETHUSDT, SOLUSDT)
   - ✅ Чекбоксы активных символов работают
   - ✅ Селектор основного символа работает
   - ✅ Кнопка "Save Symbol Settings" работает
   - ✅ После сохранения настройки применяются

### 5.2. Проверка переключателя символов

1. В верхней части страницы найдите **"Active Symbol:"** селектор
2. Переключите на разные символы:
   - BTCUSDT
   - ETHUSDT
   - SOLUSDT
3. Проверьте:
   - ✅ График обновляется при переключении
   - ✅ Статус обновляется для выбранного символа

### 5.3. Проверка графика

1. Откройте вкладку **"Dashboard"**
2. Для каждого символа:
   - Выберите символ в переключателе
   - Нажмите "Refresh Chart"
   - Проверьте:
     - ✅ Свечи загружаются
     - ✅ Индикаторы отображаются (SMA, BB, RSI, Volume, MACD)
     - ✅ Позиция (если есть) отображается
     - ✅ Сигналы отображаются

### 5.4. Проверка запуска бота

1. Убедитесь, что хотя бы один символ активен в настройках
2. Нажмите кнопку **"Start Bot"** в Dashboard
3. Проверьте логи:
   ```
   [web] MultiSymbolManager initialized with active symbols: [...]
   [MultiSymbol] ✅ Started worker thread for BTCUSDT
   [live] [BTCUSDT] Starting live trading bot
   ```
4. Проверьте статус в Dashboard:
   - ✅ Статус бота изменился на "Running"
   - ✅ Отображается информация по активным символам

### 5.5. Проверка API endpoints

Откройте терминал и выполните:

```bash
# Получить список символов
curl http://localhost:5000/api/symbols/list

# Получить активные символы
curl http://localhost:5000/api/symbols/active

# Получить статус (требует авторизации, используйте браузер или Postman)
# Откройте в браузере: http://localhost:5000/api/status

# Получить данные графика для BTCUSDT
curl "http://localhost:5000/api/chart/data?symbol=BTCUSDT"
```

---

## Шаг 6: Тестирование с несколькими символами

### 6.1. Установка нескольких активных символов

1. Откройте вкладку **"Symbols"**
2. Выберите несколько символов (например, BTCUSDT и ETHUSDT)
3. Установите PRIMARY_SYMBOL (например, BTCUSDT)
4. Нажмите **"Save Symbol Settings"**
5. Проверьте:
   - ✅ Сообщение об успешном сохранении появилось
   - ✅ Настройки применились

### 6.2. Запуск бота с несколькими символами

1. Нажмите **"Start Bot"**
2. Проверьте логи - должны запуститься воркеры для каждого активного символа:
   ```
   [MultiSymbol] ✅ Started worker thread for BTCUSDT
   [MultiSymbol] ✅ Started worker thread for ETHUSDT
   [live] [BTCUSDT] Starting live trading bot
   [live] [ETHUSDT] Starting live trading bot
   ```
3. Проверьте статус:
   - Откройте `/api/status`
   - В `symbols_status` должна быть информация по каждому символу

### 6.3. Проверка изоляции состояния

1. Каждый символ должен иметь свой файл:
   - `processed_signals_BTCUSDT.json`
   - `processed_signals_ETHUSDT.json`
   - `processed_signals_SOLUSDT.json`

2. Проверьте логи - каждый символ должен иметь свой префикс:
   ```
   [live] [BTCUSDT] ...
   [live] [ETHUSDT] ...
   [live] [SOLUSDT] ...
   ```

---

## Шаг 7: Мониторинг работы

### Проверка логов

Следите за логами в терминале. Должны быть:

1. **При запуске:**
   - `[web] MultiSymbolManager initialized with active symbols: [...]`
   - `[MultiSymbol] ✅ Started worker thread for {symbol}`

2. **При работе:**
   - `[live] [{symbol}] Starting live trading bot`
   - `[live] [{symbol}] 🔍 Checking for existing open positions...`
   - `[live] [{symbol}] 📊 TREND strategy: generated X signals`

3. **При ошибках:**
   - `[live] [{symbol}] ⚠️ Error: ...`

### Проверка статуса через API

Регулярно проверяйте статус:
```bash
# В браузере или через curl с авторизацией
http://localhost:5000/api/status
```

В ответе должна быть секция `symbols_status` с информацией по каждому активному символу.

---

## Шаг 8: Остановка бота

1. Нажмите кнопку **"Stop Bot"** в Dashboard
2. Проверьте логи:
   ```
   [MultiSymbol] 🛑 Stopping MultiSymbolManager...
   [MultiSymbol] Stopping worker for BTCUSDT...
   [MultiSymbol] ✅ MultiSymbolManager stopped
   ```
3. Проверьте статус - должен измениться на "Stopped"

---

## 📋 Чек-лист успешного тестирования

- [ ] Конфигурация проверена без ошибок
- [ ] Веб-сервер запускается без ошибок
- [ ] Вход в админку работает
- [ ] Вкладка "Symbols" отображается и работает
- [ ] Переключатель символов в верхней части работает
- [ ] График загружается для каждого символа
- [ ] API endpoints работают
- [ ] Бот запускается для одного символа
- [ ] Бот запускается для нескольких символов
- [ ] Логи показывают работу всех воркеров
- [ ] Настройки символов сохраняются в .env
- [ ] Переключение основного символа работает
- [ ] Изоляция состояния работает (отдельные файлы для каждого символа)
- [ ] Остановка бота работает корректно

---

## ⚠️ Важные замечания

1. **Используйте testnet для тестирования!**
   - Установите `BYBIT_BASE_URL=https://api-testnet.bybit.com` в .env
   - Это предотвратит использование реальных средств

2. **Проверьте права доступа:**
   - Убедитесь, что у пользователя есть права на чтение/запись `.env` файла
   - Убедитесь, что есть права на создание файлов `processed_signals_{symbol}.json`

3. **Мониторинг:**
   - Регулярно проверяйте логи на наличие ошибок
   - Следите за статусом каждого символа через API
   - Проверяйте позиции через Bybit API или интерфейс

4. **Резервное копирование:**
   - Регулярно делайте резервные копии `.env` файла
   - Сохраняйте историю сделок и сигналов

---

## 🆘 Получение помощи

Если возникли проблемы:

1. Проверьте логи в терминале
2. Проверьте `TEST_REPORT.md` для деталей тестирования
3. Проверьте `REAL_TESTING_GUIDE.md` для подробных сценариев
4. Убедитесь, что все зависимости установлены: `pip install -r requirements.txt`

---

## ✅ Готово!

После успешного прохождения всех проверок, ваш бот готов к использованию с несколькими торговыми парами одновременно!
