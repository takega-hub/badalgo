# 📝 Конфигурация Логирования

## 🎯 Управление Verbose Логами

Добавьте в `.env` для управления детализацией логов:

```env
# Уровень логирования (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Отключить веб-логи (рекомендуется для продакшена)
WEB_VERBOSE_LOGGING=false

# Отключить детали ML загрузки (рекомендуется для продакшена)
DISABLE_ML_DETAILS=true
```

---

## 📊 ЧТО БУДЕТ ЛОГИРОВАТЬСЯ:

### ✅ ВСЕГДА (важное):
- 🎯 Свежие сигналы (< 15 минут)
- 💰 Открытие/закрытие позиций
- ⚠️ Ошибки и предупреждения
- ✅ Статус воркеров
- 📊 Количество сгенерированных сигналов

### ❌ ОТКЛЮЧЕНО (verbose):
- 🔧 Детали загрузки ML моделей
- 📈 Детали подготовки фичей
- 🌐 Веб-интерфейс диагностика
- 📊 "Raw data", "After prepare", etc.

---

## 🔍 ПРОСМОТР ЛОГОВ С ФИЛЬТРАЦИЕЙ:

### Только торговые действия:
```bash
sudo journalctl -u crypto-bot -f | grep -E "💰|✅.*Selected|Opening|Closing|📊.*ML strategy.*generated"
```

### Только ошибки:
```bash
sudo journalctl -u crypto-bot -f | grep -E "ERROR|❌|⚠️"
```

### Только свежие сигналы:
```bash
sudo journalctl -u crypto-bot -f | grep "✅.*Selected.*signal"
```

### Компактный вид (без веб-логов):
```bash
sudo journalctl -u crypto-bot -f | grep -v "\[web\]"
```

---

## 🚀 ПРИМЕНЕНИЕ ИЗМЕНЕНИЙ:

После обновления `.env`:

```bash
# Перезапустите бота
sudo systemctl restart crypto-bot

# Проверьте логи
sudo journalctl -u crypto-bot -n 50 | grep -v "\[web\]"
```

---

## 💡 РЕКОМЕНДУЕМАЯ КОНФИГУРАЦИЯ:

### **Для продакшена (минимум логов):**
```env
LOG_LEVEL=INFO
WEB_VERBOSE_LOGGING=false
DISABLE_ML_DETAILS=true
```

### **Для отладки (максимум логов):**
```env
LOG_LEVEL=DEBUG
WEB_VERBOSE_LOGGING=true
DISABLE_ML_DETAILS=false
```

---

## 📋 СРАВНЕНИЕ ОБЪЕМА ЛОГОВ:

| Конфигурация | Строк логов за 1 цикл (60 сек) | Описание |
|--------------|-------------------------------|----------|
| **DEBUG (все логи)** | ~150-200 строк | Избыточно, тяжело читать |
| **INFO (рекомендуется)** | ~20-30 строк | Оптимально, видны важные события |
| **WARNING (минимум)** | ~5-10 строк | Только проблемы |

---

## 🎯 ИТОГ:

После применения этих изменений логи будут:
- ✅ **В 5-10 раз компактнее**
- ✅ **Легче читать**
- ✅ **Показывают только важное**
- ✅ **Не теряют критическую информацию**
