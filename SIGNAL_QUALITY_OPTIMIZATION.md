# Оптимизация порогов для получения 1-10 качественных сигналов в день

**Дата:** 2026-01-28

---

## 🎯 ЦЕЛЬ

Настроить пороги уверенности и фильтры так, чтобы стратегия **естественным образом** генерировала примерно **1-10 качественных сигналов в день**, без жестких ограничений.

---

## ✅ РЕАЛИЗОВАНО

### 1. Убраны жесткие ограничения

**Было:**
- После 10 сигналов все новые сигналы блокировались
- Принудительное снижение требований для достижения минимума

**Стало:**
- Нет жесткого ограничения на максимум сигналов
- Только защита от ошибок (100+ сигналов - явная ошибка)
- Фильтрация происходит через качество сигналов

### 2. Настроены пороги уверенности

**Для ансамблевых моделей:**

```python
# Пороги уверенности (умеренные для качества сигналов)
threshold_mult = 0.25-0.35  # 25-35% от стандартного (было 15-25%)
dynamic_threshold = 0.30    # 30% от стандартного (было 25%)

# Минимальная сила сигнала
min_strength = 0.005-0.007  # 0.5-0.7% (было 0.3-0.5%)
```

**Результат:**
- Более высокие требования к качеству сигналов
- Естественная фильтрация слабых сигналов
- Ожидаемое количество: 1-10 сигналов в день

### 3. Отслеживание для статистики

- Счетчик сигналов ведется для оценки работы стратегии
- Можно анализировать, сколько сигналов генерируется в день
- Используется для оптимизации порогов в будущем

---

## 📊 ТЕКУЩИЕ НАСТРОЙКИ ПОРОГОВ

### Ансамблевые модели (Quad/Triple Ensemble):

| Параметр | Волатильные (ETH/SOL) | Стабильные (BTC) |
|----------|----------------------|------------------|
| `threshold_mult` | 0.25 (25%) | 0.35 (35%) |
| `dynamic_threshold` | 0.30 (30%) | 0.30 (30%) |
| `min_strength` | 0.005 (0.5%) | 0.007 (0.7%) |

### Одиночные модели (RF/XGB):

- Используют стандартные пороги из настроек
- Более строгие требования, чем у ансамблей

---

## 🔧 КАК НАСТРОИТЬ ПОД СВОИ НУЖДЫ

### Если сигналов слишком мало (< 1 в день):

**Снизить пороги:**
```python
# В bot/ml/strategy_ml.py
threshold_mult = 0.20 if is_volatile_symbol else 0.30  # Снизить на 5%
dynamic_threshold = self.confidence_threshold * 0.25     # Снизить на 5%
min_strength = 0.004 if is_volatile_symbol else 0.006   # Снизить на 0.1%
```

### Если сигналов слишком много (> 10 в день):

**Повысить пороги:**
```python
# В bot/ml/strategy_ml.py
threshold_mult = 0.30 if is_volatile_symbol else 0.40  # Повысить на 5%
dynamic_threshold = self.confidence_threshold * 0.35   # Повысить на 5%
min_strength = 0.006 if is_volatile_symbol else 0.008  # Повысить на 0.1%
```

### Если сигналы низкого качества:

**Повысить все пороги:**
- Увеличить `threshold_mult` на 10-15%
- Увеличить `dynamic_threshold` на 5-10%
- Увеличить `min_strength` на 0.1-0.2%

---

## 📈 МОНИТОРИНГ И ОЦЕНКА

### Метрики для отслеживания:

1. **Количество сигналов в день:**
   - Целевой диапазон: 1-10 сигналов
   - Если меньше 1 - снизить пороги
   - Если больше 10 - повысить пороги

2. **Качество сигналов:**
   - Win Rate (должен быть > 50%)
   - Profit Factor (должен быть > 1.0)
   - Средняя уверенность сигналов (должна быть > 60%)

3. **Распределение сигналов:**
   - Равномерное распределение в течение дня
   - Нет скоплений сигналов в короткие периоды

### Рекомендации:

- **Через 1 неделю:** Проверить среднее количество сигналов в день
- **Через 2 недели:** Запустить `compare_ml_models.py` для оценки качества
- **Через 1 месяц:** Оптимизировать пороги на основе реальных результатов

---

## 🎯 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

После оптимизации порогов ожидается:

- ✅ **1-10 качественных сигналов в день** естественным образом
- ✅ **Win Rate > 50%** за счет более высоких требований к качеству
- ✅ **Profit Factor > 1.0** за счет фильтрации слабых сигналов
- ✅ **Равномерное распределение** сигналов в течение дня

---

## 📝 ПРИМЕЧАНИЯ

- Пороги можно настраивать индивидуально для каждого символа
- Рекомендуется тестировать изменения на исторических данных перед применением
- Регулярно пересматривать пороги на основе реальных результатов
- Использовать статистику сигналов для принятия решений об оптимизации
