# 📥 ИНСТРУКЦИЯ ПО ЗАГРУЗКЕ ДАННЫХ

## 🚀 Быстрый старт

### Загрузка всех данных для MTF (рекомендуется)

```bash
# Загрузить BTC, ETH, SOL со всеми таймфреймами (15m, 1h, 4h)
python bot/download_data.py --all

# С указанием количества дней истории
python bot/download_data.py --all --days 180
```

### Загрузка данных для одного символа

```bash
# BTC со всеми таймфреймами (15m, 1h, 4h)
python bot/download_data.py --symbol BTC/USDT

# ETH со всеми таймфреймами
python bot/download_data.py --symbol ETH/USDT

# SOL со всеми таймфреймами
python bot/download_data.py --symbol SOL/USDT
```

### Загрузка конкретной комбинации

```bash
# BTC 15m
python bot/download_data.py --symbol BTC/USDT --timeframe 15m

# ETH 1h
python bot/download_data.py --symbol ETH/USDT --timeframe 1h

# SOL 4h
python bot/download_data.py --symbol SOL/USDT --timeframe 4h
```

## 📋 Параметры

- `--symbol` - Торговая пара (например, `BTC/USDT`, `ETH/USDT`, `SOL/USDT`)
- `--timeframe` - Таймфрейм (`15m`, `1h`, `4h`, `1d`)
- `--days` - Количество дней истории (по умолчанию 365)
- `--all` - Загрузить все символы и таймфреймы
- `--output` - Директория для сохранения (по умолчанию `data`)

## 📁 Структура файлов

После загрузки файлы будут сохранены в формате:
```
data/
├── btcusdt_15m.csv
├── btcusdt_1h.csv
├── btcusdt_4h.csv
├── ethusdt_15m.csv
├── ethusdt_1h.csv
├── ethusdt_4h.csv
├── solusdt_15m.csv
├── solusdt_1h.csv
└── solusdt_4h.csv
```

## 📊 Формат данных

Каждый CSV файл содержит следующие колонки:
- `timestamp` - Unix timestamp в миллисекундах
- `open` - Цена открытия
- `high` - Максимальная цена
- `low` - Минимальная цена
- `close` - Цена закрытия
- `volume` - Объем торгов
- `datetime` - Дата и время в читаемом формате

## ⚙️ Примеры использования

### Пример 1: Загрузка данных для обучения MTF модели

```bash
# Загружаем 1 год истории для всех символов
python bot/download_data.py --all --days 365
```

### Пример 2: Обновление данных для конкретного символа

```bash
# Обновляем только BTC данные
python bot/download_data.py --symbol BTC/USDT --days 30
```

### Пример 3: Загрузка в другую директорию

```bash
python bot/download_data.py --all --output ./my_data
```

## ⚠️ Важные замечания

1. **Rate Limits**: Скрипт автоматически делает паузы между запросами для избежания блокировки API
2. **Время загрузки**: Загрузка всех данных может занять 10-20 минут в зависимости от скорости интернета
3. **Размер данных**: 1 год данных для всех символов и таймфреймов занимает примерно 50-100 МБ
4. **Повторы**: При ошибках сети скрипт автоматически повторяет попытки (до 3 раз)

## 🔧 Использование в коде

```python
from bot.download_data import download_history, download_mtf_data

# Загрузка одной комбинации
df = download_history('BTC/USDT', '15m', days=365)

# Загрузка всех MTF данных
results = download_mtf_data(
    symbols=['BTC/USDT', 'ETH/USDT'],
    timeframes=['15m', '1h', '4h'],
    days=365
)

# Доступ к данным
btc_15m = results['BTC/USDT']['15m']
eth_1h = results['ETH/USDT']['1h']
```

## 📈 Рекомендации

- **Для обучения MTF модели**: Загрузите минимум 180-365 дней данных
- **Для бектестинга**: Загрузите данные за период, который не использовался для обучения
- **Для live торговли**: Обновляйте данные регулярно (раз в день/неделю)
