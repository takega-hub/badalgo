# Руководство по настройкам ML стратегии

## Обзор настроек

В админке доступны 3 основные настройки для корректировки количества и точности сигналов ML стратегии:

1. **ML Confidence Threshold (0-1)** - Минимальная уверенность ML модели
2. **ML Min Signal Strength** - Минимальная сила сигнала
3. **ML Stability Filter** - Фильтр стабильности для смены направления

---

## 1. ML Confidence Threshold (0-1)

**Текущее значение:** 0.4 (40%)

### Что это делает:
Базовый порог уверенности модели для открытия позиции. Модель должна показать уверенность выше этого значения, чтобы сигнал был принят.

### Как влияет на сигналы:

#### Для одиночных моделей (RF, XGB, LSTM):
- **Низкое значение (0.2-0.3):** 
  - ✅ Больше сигналов
  - ⚠️ Меньше точность (больше ложных сигналов)
  - Используется как: `confidence_threshold * 0.70-0.85` (для волатильных/стабильных символов)

- **Среднее значение (0.4-0.5):**
  - ✅ Баланс между количеством и точностью
  - ✅ Рекомендуется для большинства случаев

- **Высокое значение (0.6-0.8):**
  - ✅ Высокая точность (меньше ложных сигналов)
  - ⚠️ Меньше сигналов (может пропустить хорошие возможности)

#### Для ансамблей (triple_ensemble, quad_ensemble, ensemble):
- **Ансамбли используют очень низкий порог автоматически - СЛАБЫЕ СИГНАЛЫ НЕ ИГНОРИРУЮТСЯ:**
  - `confidence_threshold * 0.01` (1% от заданного значения)
  - Например, при `confidence_threshold = 0.4` → реальный порог = `0.004` (0.4%)
  - Это сделано для максимального количества сигналов от ансамблей
  - **Слабые сигналы от ансамблей принимаются и исполняются**

### Рекомендации:
- **Для ансамблей:** 0.3-0.4 (они и так используют очень низкий порог)
- **Для одиночных моделей:** 0.4-0.6
- **Для агрессивной торговли:** 0.2-0.3
- **Для консервативной торговли:** 0.6-0.8

---

## 2. ML Min Signal Strength

**Текущее значение:** "Умеренное (60%)"

### Варианты:
- **"слабое"** → порог 0.0% (принимает все сигналы)
- **"умеренное"** → порог 60%
- **"среднее"** → порог 70%
- **"сильное"** → порог 80%
- **"очень_сильное"** → порог 90%

### Что это делает:
Дополнительный фильтр по силе сигнала. Даже если модель уверена, сигнал должен пройти этот порог.

### Как влияет на сигналы:

#### Для одиночных моделей:
- **"слабое" (0%):**
  - ✅ Максимальное количество сигналов
  - ⚠️ Много слабых сигналов

- **"умеренное" (60%):**
  - ✅ Баланс (рекомендуется)
  - ✅ Отфильтровывает очень слабые сигналы

- **"среднее" (70%):**
  - ✅ Хорошая точность
  - ⚠️ Меньше сигналов

- **"сильное" (80-90%):**
  - ✅ Высокая точность
  - ⚠️ Очень мало сигналов

#### Для ансамблей:
- **Ансамбли используют очень низкий порог автоматически:**
  - Для волатильных символов: 1% (почти отключен)
  - Для стабильных символов: 2% (почти отключен)
  - Настройка `min_signal_strength` почти не влияет на ансамбли

### Рекомендации:
- **Для ансамблей:** "слабое" или "умеренное" (все равно используется низкий порог)
- **Для одиночных моделей:** "умеренное" или "среднее"
- **Для агрессивной торговли:** "слабое"
- **Для консервативной торговли:** "сильное" или "очень_сильное"

---

## 3. ML Stability Filter

**Текущее значение:** Выключен (unchecked)

### Что это делает:
Требует более высокую уверенность для смены направления позиции. Если у вас открыта LONG позиция, и модель предсказывает SHORT, фильтр требует повышенную уверенность.

### Как влияет на сигналы:

#### Когда включен:
- **Для ансамблей:**
  - Порог стабильности: 5% (почти отключен)
  - Почти не влияет на сигналы

- **Для одиночных моделей:**
  - **Волатильные символы (ETH, SOL):** `confidence_threshold * 0.70` (минимум 35%)
  - **Стабильные символы (BTC):** `confidence_threshold * 0.85` (минимум 45%)
  - ✅ Уменьшает количество переворотов позиций
  - ✅ Повышает стабильность торговли
  - ⚠️ Может пропустить хорошие возможности для переворота

#### Когда выключен:
- ✅ Больше сигналов на смену направления
- ⚠️ Больше переворотов позиций (может увеличить комиссии)

### Рекомендации:
- **Для ансамблей:** Можно оставить выключенным (все равно почти не влияет)
- **Для одиночных моделей:** 
  - **Включить** - если хотите меньше переворотов, более стабильную торговлю
  - **Выключить** - если хотите больше сигналов, более агрессивную торговлю

---

## Комбинации настроек для разных целей

### 🎯 Максимальное количество сигналов:
```
ML Confidence Threshold: 0.2-0.3
ML Min Signal Strength: "слабое"
ML Stability Filter: Выключен
```
**Результат:** Максимум сигналов, но больше ложных срабатываний

### ⚖️ Баланс (рекомендуется):
```
ML Confidence Threshold: 0.4-0.5
ML Min Signal Strength: "умеренное"
ML Stability Filter: Включен (для одиночных моделей)
```
**Результат:** Хороший баланс между количеством и точностью

### 🎯 Высокая точность:
```
ML Confidence Threshold: 0.6-0.7
ML Min Signal Strength: "сильное" или "очень_сильное"
ML Stability Filter: Включен
```
**Результат:** Меньше сигналов, но высокая точность

### 🚀 Агрессивная торговля:
```
ML Confidence Threshold: 0.2-0.3
ML Min Signal Strength: "слабое"
ML Stability Filter: Выключен
```
**Результат:** Максимум сигналов, быстрые перевороты

### 🛡️ Консервативная торговля:
```
ML Confidence Threshold: 0.7-0.8
ML Min Signal Strength: "очень_сильное"
ML Stability Filter: Включен
```
**Результат:** Только очень уверенные сигналы, минимум переворотов

---

## Особенности для разных типов моделей

### 🚀 Ансамбли (TripleEnsemble, QuadEnsemble, ensemble):
- **Автоматически используют очень низкие пороги - СЛАБЫЕ СИГНАЛЫ НЕ ИГНОРИРУЮТСЯ:**
  - `confidence_threshold * 0.01` (1% от заданного значения)
  - `min_strength = 0.1-0.5%` (почти полностью отключен)
  - `stability_filter` почти не влияет (порог 0.1%)

- **Рекомендации:**
  - Можно ставить средние значения настроек - они все равно будут снижены
  - Ансамбли дают больше сигналов по умолчанию
  - Система автоматически адаптирует настройки для ансамблей

### Одиночные модели (RF, XGB, LSTM):
- **Используют настройки напрямую:**
  - `confidence_threshold * 0.70-0.85` (для волатильных/стабильных)
  - `min_strength_threshold` применяется как есть
  - `stability_filter` работает полноценно

- **Рекомендации:**
  - Нужно более аккуратно настраивать параметры
  - Более чувствительны к изменениям настроек

---

## 🎯 Рекомендуемые настройки для TripleEnsemble и QuadEnsemble

### ✅ Оптимальная конфигурация:

```
ML Confidence Threshold: 0.4
ML Min Signal Strength: "Умеренное (60%)"
ML Stability Filter: ✅ Включен (опционально)
```

### 📊 Как это работает:

#### 1. ML Confidence Threshold = 0.4
- **Реальный порог для ансамбля:** `0.4 * 0.01 = 0.004` (0.4%) - очень низкий
- **Результат:** Слабые сигналы от ансамблей НЕ игнорируются, почти все сигналы принимаются
- **Альтернативы:**
  - `0.3` → реальный порог = 0.3% (максимум сигналов)
  - `0.5` → реальный порог = 0.5% (чуть меньше сигналов)

#### 2. ML Min Signal Strength = "Умеренное (60%)"
- **Реальный порог для ансамбля:** 0.1-0.5% (почти полностью отключен)
- **Результат:** Настройка почти не влияет на ансамбли, слабые сигналы НЕ блокируются
- **Можно выбрать любое значение** - система использует фиксированный порог:
  - Волатильные символы (ETH, SOL): 0.1% (почти отключен)
  - Стабильные символы (BTC): 0.5% (почти отключен)

#### 3. ML Stability Filter = Включен
- **Реальный порог для ансамбля:** 0.1% (почти полностью отключен)
- **Результат:** Почти не блокирует сигналы, слабые сигналы НЕ игнорируются
- **Рекомендация:** Можно включить, но он почти не влияет на ансамбли

### 📈 Примеры конфигураций:

#### Максимум сигналов (агрессивная торговля):
```
ML Confidence Threshold: 0.3
ML Min Signal Strength: "слабое"
ML Stability Filter: Выключен
```
**Реальный порог:** ~0.3% | **Сигналов:** Максимум (слабые сигналы НЕ игнорируются)

#### Баланс (рекомендуется):
```
ML Confidence Threshold: 0.4
ML Min Signal Strength: "умеренное"
ML Stability Filter: Включен
```
**Реальный порог:** ~0.4% | **Сигналов:** Много (слабые сигналы принимаются)

#### Высокая точность (консервативная торговля):
```
ML Confidence Threshold: 0.5-0.6
ML Min Signal Strength: "среднее"
ML Stability Filter: Включен
```
**Реальный порог:** ~0.5-0.6% | **Сигналов:** Много (слабые сигналы все равно принимаются)

### ⚠️ Важно понимать:

1. **Автоматическая адаптация:**
   - Система автоматически снижает пороги для ансамблей
   - Можно использовать средние значения - они будут адаптированы

2. **Почему низкие пороги:**
   - Ансамбли объединяют несколько моделей
   - Вероятности распределяются более равномерно
   - Низкие пороги позволяют получить максимум сигналов

3. **Min Signal Strength почти не влияет:**
   - Для ансамблей используется фиксированный порог 0.1-0.5%
   - Настройка в админке почти не влияет на результат
   - **Слабые сигналы НЕ блокируются**

4. **Stability Filter почти отключен:**
   - Порог 0.1% для смены направления (почти полностью отключен)
   - **Слабые сигналы НЕ блокируются**, даже при смене направления

---

## Как тестировать настройки

1. **Используйте кнопку "Test ML Strategy"** в админке
2. **Смотрите результаты:**
   - Количество сигналов
   - Win Rate (процент прибыльных сделок)
   - Total PnL (общая прибыль/убыток)
   - Средняя прибыль на сделку

3. **Итеративно настраивайте:**
   - Если сигналов мало → снижайте пороги
   - Если много ложных сигналов → повышайте пороги
   - Если много переворотов → включите Stability Filter

---

## Важные замечания

1. **Ансамбли vs Одиночные модели:**
   - **TripleEnsemble / QuadEnsemble:** Автоматически используют пороги в 5% от заданного значения
   - **Одиночные модели:** Используют пороги в 70-85% от заданного значения
   - Настройки для ансамблей имеют меньшее влияние, но все равно важны

2. **Волатильные символы (ETH, SOL):**
   - Автоматически используют более низкие пороги
   - Больше сигналов, но может быть больше ложных срабатываний

3. **Стабильные символы (BTC):**
   - Используют стандартные пороги
   - Меньше сигналов, но выше точность

4. **Динамические пороги:**
   - Стратегия автоматически корректирует пороги на основе рыночных условий
   - Учитывает волатильность, тренд, объем

---

## Примеры реальных значений

### Для triple_ensemble_BTCUSDT_15.pkl или quad_ensemble_BTCUSDT_15.pkl:
```
ML Confidence Threshold: 0.4
ML Min Signal Strength: "умеренное"
ML Stability Filter: Включен (опционально, почти не влияет)
```
**Реальный порог:** 0.4-0.5% | **Ожидаемый результат:** ~15-30 сигналов в день (слабые сигналы НЕ игнорируются)

### Для triple_ensemble_ETHUSDT_15.pkl или quad_ensemble_ETHUSDT_15.pkl:
```
ML Confidence Threshold: 0.4
ML Min Signal Strength: "умеренное"
ML Stability Filter: Включен (опционально, почти не влияет)
```
**Реальный порог:** 0.1-0.4% | **Ожидаемый результат:** ~20-35 сигналов в день (слабые сигналы НЕ игнорируются)

### Для rf_ETHUSDT_15.pkl (одиночная модель):
```
ML Confidence Threshold: 0.5
ML Min Signal Strength: "среднее"
ML Stability Filter: Включен
```
**Ожидаемый результат:** ~5-10 сигналов в день, высокая точность

---

## Мониторинг и оптимизация

1. **Отслеживайте метрики:**
   - Win Rate должен быть > 50% для прибыльной торговли
   - Количество сигналов должно быть достаточным для диверсификации
   - Total PnL должен быть положительным

2. **Корректируйте настройки:**
   - Если Win Rate < 40% → повышайте пороги
   - Если сигналов слишком мало → снижайте пороги
   - Если много переворотов → включите Stability Filter

3. **Переобучение модели:**
   - Если модель старая (>30 дней) → переобучите
   - Если Win Rate низкий → переобучите с новыми данными
