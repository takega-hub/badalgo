# 🚀 LSTM ML Стратегия - Четвертая ML Стратегия

## 📋 ОБЗОР

**LSTM (Long Short-Term Memory)** - это четвертая ML стратегия, которая дополняет существующие:
1. ✅ **Ensemble (RF + XGBoost)** - классификатор на фичах
2. ✅ **LightGBM** - быстрый градиентный бустинг
3. ⏳ **RL (PPO)** - обучение с подкреплением (в разработке)
4. 🆕 **LSTM** - нейросеть для временных рядов

---

## 🎯 ПОЧЕМУ LSTM?

### Преимущества LSTM:

1. **⏰ Временные ряды**
   - Крипто - это чистейший временной ряд
   - LSTM улавливает долгосрочные зависимости (50-100 свечей)
   - Помнит важную информацию на длинных периодах

2. **🔍 Дополняет существующие модели**
   - Классификаторы (RF/XGB/LGB) работают с фичами
   - LSTM работает с последовательностями свечей
   - Разные подходы = разные паттерны

3. **📈 Лучше находит тренды**
   - Улавливает циклические паттерны
   - Определяет моменты разворота тренда
   - Работает на долгосрочных периодах

4. **🎯 Высокая точность**
   - Ожидаемое улучшение: +3-7% по сравнению с классификаторами
   - Лучше на волатильных рынках
   - Меньше ложных сигналов

---

## 🏗️ АРХИТЕКТУРА

### Структура модели:

```
Input: Последовательность из 60 свечей
    ↓
Каждая свеча содержит: [close, volume, rsi, macd, adx, atr, ...]
    ↓
LSTM Layer 1 (64 нейронов)
    ↓
LSTM Layer 2 (64 нейрона)
    ↓
Dropout (0.2) для регуляризации
    ↓
Dense Layer (3 класса)
    ↓
Output: [SHORT, HOLD, LONG] с вероятностями
```

### Параметры по умолчанию:

- **Sequence Length:** 60 свечей (15 часов на 15m таймфрейме)
- **Hidden Size:** 64 нейрона
- **Layers:** 2 LSTM слоя
- **Dropout:** 0.2
- **Learning Rate:** 0.001
- **Batch Size:** 32

---

## 📦 УСТАНОВКА

### 1. Установить PyTorch:
```bash
pip install torch>=2.0.0
```

Или обновить requirements.txt (уже добавлено):
```bash
pip install -r requirements.txt
```

### 2. Проверить установку:
```python
import torch
print(f"PyTorch version: {torch.__version__}")
print(f"CUDA available: {torch.cuda.is_available()}")
```

---

## 🎓 ОБУЧЕНИЕ МОДЕЛИ

### Базовое обучение:

```bash
python train_lstm_model.py --symbol BTCUSDT --days 180
```

### С кастомными параметрами:

```bash
python train_lstm_model.py \
    --symbol BTCUSDT \
    --days 180 \
    --sequence_length 60 \
    --hidden_size 128 \
    --num_layers 3 \
    --epochs 100 \
    --batch_size 64 \
    --learning_rate 0.0005
```

### Обучение для всех символов:

```bash
for symbol in BTCUSDT ETHUSDT SOLUSDT; do
    python train_lstm_model.py --symbol $symbol --days 180
done
```

---

## 🔍 КАК РАБОТАЕТ LSTM

### 1. Подготовка данных:

LSTM требует последовательности свечей, а не отдельные фичи:

```python
# Классификаторы работают так:
features = [rsi, macd, adx, ...]  # Одна свеча
prediction = model.predict(features)

# LSTM работает так:
sequence = [
    [close, volume, rsi, macd, ...],  # Свеча 1
    [close, volume, rsi, macd, ...],  # Свеча 2
    ...
    [close, volume, rsi, macd, ...],  # Свеча 60
]
prediction = lstm_model.predict(sequence)
```

### 2. Анализ последовательности:

LSTM анализирует всю последовательность целиком:
- Видит паттерны на 60 свечах
- Запоминает важные события
- Улавливает долгосрочные зависимости

### 3. Предсказание:

Модель предсказывает движение цены на основе:
- Исторических паттернов
- Текущего состояния рынка
- Долгосрочных трендов

---

## 📊 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

### Метрики:

| Метрика | Классификаторы | LSTM | Улучшение |
|---------|----------------|------|-----------|
| **CV Accuracy** | 0.80 | 0.83-0.87 | +3-7% |
| **F1-Score** | 0.80 | 0.83-0.87 | +3-7% |
| **Долгосрочные тренды** | Средне | Отлично | +++ |
| **Волатильность** | Хорошо | Отлично | ++ |

### Преимущества:

1. **Больше сигналов на трендах**
   - Лучше находит долгосрочные движения
   - Меньше ложных сигналов на флэте

2. **Лучше на волатильности**
   - Улавливает циклические паттерны
   - Определяет моменты разворота

3. **Стабильность**
   - Меньше переобучения
   - Лучше обобщает на новых данных

---

## 🔄 ИНТЕГРАЦИЯ С СУЩЕСТВУЮЩИМИ МОДЕЛЯМИ

### Гибридный подход:

Можно комбинировать LSTM с классификаторами:

```python
# Получаем предсказания от разных моделей
ensemble_pred = ensemble_model.predict(features)      # RF+XGB+LGB
lstm_pred = lstm_model.predict(sequence)             # LSTM

# Взвешенное усреднение
final_pred = 0.6 * ensemble_pred + 0.4 * lstm_pred
```

### Когда использовать LSTM:

- ✅ **Долгосрочные тренды** - LSTM лучше
- ✅ **Волатильные рынки** - LSTM лучше
- ✅ **Циклические паттерны** - LSTM лучше

### Когда использовать классификаторы:

- ✅ **Краткосрочные сигналы** - классификаторы быстрее
- ✅ **Стабильные рынки** - классификаторы точнее
- ✅ **Быстрые решения** - классификаторы быстрее

---

## 🧪 ТЕСТИРОВАНИЕ

### 1. Быстрый тест:

```python
from bot.ml.lstm_model import LSTMTrainer
import pandas as pd

# Загрузить данные
df = pd.read_csv('data/btcusdt_15m.csv')

# Создать фичи и таргет
feature_engineer = FeatureEngineer()
df_features = feature_engineer.create_technical_indicators(df)
df_target = feature_engineer.create_target_variable(df_features)

# Обучить модель
trainer = LSTMTrainer(sequence_length=60)
model, metrics = trainer.train(df_target)

print(f"Validation Accuracy: {metrics['best_val_acc']:.4f}")
```

### 2. Бэктестинг:

После обучения модели, можно протестировать на исторических данных:

```python
# Загрузить модель
model, model_data = LSTMTrainer.load_model('ml_models/lstm_BTCUSDT_15.pkl')

# Подготовить последовательности
sequences, targets = trainer.prepare_sequences(df_test)

# Предсказания
predictions, probabilities = trainer.predict(sequences)

# Оценить точность
accuracy = (predictions == targets).mean()
print(f"Test Accuracy: {accuracy:.4f}")
```

---

## 📈 ОПТИМИЗАЦИЯ ПАРАМЕТРОВ

### Рекомендуемые параметры для разных символов:

#### BTCUSDT (стабильный):
```python
{
    "sequence_length": 60,
    "hidden_size": 64,
    "num_layers": 2,
    "learning_rate": 0.001,
}
```

#### ETHUSDT (волатильный):
```python
{
    "sequence_length": 80,      # Длиннее последовательность
    "hidden_size": 128,          # Больше нейронов
    "num_layers": 3,             # Больше слоев
    "learning_rate": 0.0005,     # Меньше learning rate
}
```

#### SOLUSDT (очень волатильный):
```python
{
    "sequence_length": 100,      # Очень длинная последовательность
    "hidden_size": 128,
    "num_layers": 3,
    "learning_rate": 0.0005,
    "dropout": 0.3,              # Больше dropout
}
```

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

### 1. Обучение моделей (Приоритет: ВЫСОКИЙ)
- [ ] Обучить LSTM для BTCUSDT
- [ ] Обучить LSTM для ETHUSDT
- [ ] Обучить LSTM для SOLUSDT
- [ ] Сравнить результаты с классификаторами

### 2. Бэктестинг (Приоритет: ВЫСОКИЙ)
- [ ] Протестировать на исторических данных (30 дней)
- [ ] Сравнить метрики (Win Rate, Profit Factor)
- [ ] Проанализировать типы сигналов

### 3. Интеграция (Приоритет: СРЕДНИЙ)
- [ ] Добавить поддержку LSTM в `strategy_ml.py`
- [ ] Интегрировать в `live.py`
- [ ] Добавить в веб-интерфейс

### 4. Гибридный подход (Приоритет: НИЗКИЙ)
- [ ] Комбинировать LSTM с классификаторами
- [ ] Оптимизировать веса ансамбля
- [ ] Тестировать на разных таймфреймах

---

## 📚 ДОПОЛНИТЕЛЬНАЯ ИНФОРМАЦИЯ

### Полезные ссылки:
- [PyTorch LSTM Documentation](https://pytorch.org/docs/stable/generated/torch.nn.LSTM.html)
- [Understanding LSTM Networks](https://colah.github.io/posts/2015-08-Understanding-LSTMs/)

### Ключевые файлы:
- `bot/ml/lstm_model.py` - реализация LSTM модели
- `train_lstm_model.py` - скрипт для обучения
- `requirements.txt` - добавлен `torch>=2.0.0`

---

## ✅ ЗАКЛЮЧЕНИЕ

**LSTM** - отличное дополнение к существующим ML стратегиям:
- ✅ Идеально для временных рядов (крипто)
- ✅ Дополняет классификаторы
- ✅ Улавливает долгосрочные зависимости
- ✅ Ожидаемое улучшение: +3-7% точности

**Рекомендация:** Начать с обучения LSTM для всех символов и сравнить результаты с существующими моделями.
