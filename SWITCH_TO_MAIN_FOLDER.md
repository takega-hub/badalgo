# 🔄 Переключение с worktree на основную папку

## Текущая ситуация

Вы работаете в **worktree** (`C:\Users\takeg\.cursor\worktrees\crypto_bot\vib\`), но хотите работать напрямую в **основной папке** (`C:\Users\takeg\OneDrive\Документы\vibecodding\crypto_bot\`).

## ⚠️ Важно перед удалением worktree

1. **Все изменения уже синхронизированы** - worktree использует тот же git репозиторий, поэтому закоммиченные изменения видны везде
2. **Незакоммиченные изменения** нужно либо закоммитить, либо скопировать вручную

## 📋 Шаги для переключения

### Вариант 1: Автоматический (рекомендуется)

1. **Откройте PowerShell в основной папке**:
   ```powershell
   cd "C:\Users\takeg\OneDrive\Документы\vibecodding\crypto_bot"
   ```

2. **Запустите скрипт удаления worktree**:
   ```powershell
   .\remove_worktree.ps1
   ```

3. **Перезапустите Cursor** - закройте и откройте заново, указав основную папку как workspace

### Вариант 2: Ручной

1. **Откройте PowerShell в основной папке**:
   ```powershell
   cd "C:\Users\takeg\OneDrive\Документы\vibecodding\crypto_bot"
   ```

2. **Проверьте список worktree**:
   ```powershell
   git worktree list
   ```

3. **Удалите каждый worktree**:
   ```powershell
   git worktree remove "C:\Users\takeg\.cursor\worktrees\crypto_bot\vib" --force
   git worktree remove "C:\Users\takeg\.cursor\worktrees\crypto_bot\jjh" --force
   git worktree remove "C:\Users\takeg\.cursor\worktrees\crypto_bot\omu" --force
   git worktree remove "C:\Users\takeg\.cursor\worktrees\crypto_bot\xdf" --force
   ```

4. **Проверьте, что worktree удалены**:
   ```powershell
   git worktree list
   ```
   Должна остаться только основная папка.

5. **Перезапустите Cursor** и откройте основную папку как workspace

## 🔍 Проверка синхронизации файлов

Перед удалением worktree убедитесь, что важные файлы синхронизированы:

```powershell
# Сравните ключевые файлы (если нужно)
Compare-Object (Get-Content "C:\Users\takeg\.cursor\worktrees\crypto_bot\vib\bot\train_v17_optimized.py") (Get-Content "C:\Users\takeg\OneDrive\Документы\vibecodding\crypto_bot\bot\train_v17_optimized.py")
```

## ✅ После переключения

1. **Откройте Cursor** и выберите основную папку как workspace:
   ```
   C:\Users\takeg\OneDrive\Документы\vibecodding\crypto_bot
   ```

2. **Проверьте, что файлы на месте**:
   ```powershell
   cd "C:\Users\takeg\OneDrive\Документы\vibecodding\crypto_bot"
   ls bot\train_v17_optimized.py
   ```

3. **Проверьте git статус**:
   ```powershell
   git status
   ```

## 🎯 Преимущества работы в основной папке

- ✅ Все изменения сразу в нужном месте
- ✅ Нет путаницы с путями
- ✅ Проще работать с файлами
- ✅ Меньше проблем с синхронизацией

## ⚠️ Если что-то пошло не так

Если после удаления worktree вы потеряли какие-то изменения:

1. **Проверьте git reflog**:
   ```powershell
   git reflog
   ```

2. **Восстановите изменения**:
   ```powershell
   git checkout <commit-hash> -- <file-path>
   ```

3. **Или восстановите worktree** (если папка еще существует):
   ```powershell
   git worktree add <path> <commit-hash>
   ```
