# 🚀 Deploy Changes to Server

## 📝 Что было изменено:

### 1. Исправлена генерация сигналов
- **Понижен порог ADX** для Momentum стратегии: `25.0` → `20.0`
- **Увеличен лимит volume** для FLAT стратегии: `1.3x` → `2.0x`
- **Добавлен допуск для касания BB**: `0.002` (0.2%)
- **Включена TREND стратегия** по умолчанию

### 2. Добавлена загрузка параметров из .env
- Добавлена загрузка `MOMENTUM_ADX_THRESHOLD` в `bot/config.py`
- Добавлено сохранение `MOMENTUM_ADX_THRESHOLD` в `bot/web/app.py`
- Добавлена загрузка `RANGE_BB_TOUCH_TOLERANCE_PCT` в `bot/config.py`
- Добавлено сохранение `RANGE_BB_TOUCH_TOLERANCE_PCT` в `bot/web/app.py`

### 3. Улучшена логика FLAT стратегии
- Обновлена логика касания BB с допуском в `bot/strategy.py`

### 4. Добавлены диагностические скрипты
- `verify_indicators.py` - проверка индикаторов
- `trace_signal_generation.py` - трассировка генерации сигналов
- `fix_strategy_settings.py` - автоматическое обновление настроек

---

## 🔄 Деплой на локальной машине (Windows)

### Вариант 1: Через PowerShell скрипт

```powershell
.\deploy_to_server.ps1
```

### Вариант 2: Вручную

```powershell
# 1. Проверка изменений
git status

# 2. Добавить все изменения
git add .

# 3. Коммит
git commit -m "Fix strategy signal generation: lower ADX threshold, increase volume limit, add BB touch tolerance"

# 4. Push в GitHub
git push origin main
```

---

## 🖥️ Деплой на удаленном сервере (Linux)

### 1. Подключитесь к серверу

```bash
ssh user@your-server-ip
```

### 2. Перейдите в директорию проекта

```bash
cd /path/to/crypto_bot
# Например: cd ~/crypto_bot
```

### 3. Получите изменения из GitHub

```bash
git pull origin main
```

### 4. Обновите настройки (автоматически)

```bash
python3 fix_strategy_settings.py
```

Или **вручную** отредактируйте `.env`:

```bash
nano .env
```

Измените/добавьте:
```env
MOMENTUM_ADX_THRESHOLD=20.0
RANGE_VOLUME_MULT=2.0
RANGE_BB_TOUCH_TOLERANCE_PCT=0.002
ENABLE_TREND_STRATEGY=true
```

### 5. Перезапустите бота

#### Если используется systemd:

```bash
sudo systemctl restart crypto-bot
sudo systemctl status crypto-bot
```

#### Если используется supervisor:

```bash
sudo supervisorctl restart crypto-bot
sudo supervisorctl status crypto-bot
```

#### Если запущен вручную:

```bash
# Найдите процесс
ps aux | grep python | grep main.py

# Убейте процесс
kill <PID>

# Запустите снова
cd ~/crypto_bot
source venv/bin/activate
python main.py &
```

### 6. Проверьте логи

```bash
# Для systemd
sudo journalctl -u crypto-bot -f

# Для supervisor
sudo tail -f /var/log/crypto-bot/crypto-bot.log

# Для вручную запущенного
tail -f nohup.out
```

### 7. Проверьте веб-интерфейс

Откройте в браузере:
```
http://your-server-ip:5002
```

Проверьте вкладку **"Signal History"** - должны появиться новые сигналы!

---

## ✅ Ожидаемый результат

После обновления бот должен:

1. ✅ Генерировать **MOMENTUM сигналы** при ADX >= 20 (вместо 25)
2. ✅ Генерировать **FLAT SHORT сигналы** при перекупленности с volume до 2.0x
3. ✅ Срабатывать при **приближении к границам BB** (допуск 0.2%)
4. ✅ Генерировать **TREND сигналы** (стратегия включена)

---

## 🐛 Troubleshooting

### Проблема: Git pull выдает конфликты

```bash
# Откатить локальные изменения в .env
git checkout .env

# Повторить pull
git pull origin main
```

### Проблема: Бот не перезапускается

```bash
# Проверить статус
sudo systemctl status crypto-bot

# Посмотреть логи
sudo journalctl -u crypto-bot -n 50
```

### Проблема: Нет новых сигналов

1. Проверьте настройки через веб-интерфейс
2. Запустите диагностику:
   ```bash
   python3 verify_indicators.py
   python3 trace_signal_generation.py
   ```

---

## 📞 Support

Если возникли проблемы, проверьте:
- Логи бота
- Статус сервиса
- Настройки в `.env`
- Веб-интерфейс на `http://server-ip:5002`
