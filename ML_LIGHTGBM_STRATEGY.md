# 🚀 LightGBM ML Стратегия - Третья ML Стратегия

## 📋 ОБЗОР

**LightGBM** - это третья ML стратегия, которая дополняет существующие:
1. ✅ **Ensemble (RF + XGBoost)** - рабочая классификационная модель
2. ⏳ **RL (PPO)** - стратегия обучения с подкреплением (в разработке)
3. 🆕 **LightGBM** - быстрый и эффективный градиентный бустинг

---

## 🎯 ПОЧЕМУ LIGHTGBM?

### Преимущества LightGBM:

1. **⚡ Скорость**
   - Обучается в 2-10 раз быстрее XGBoost
   - Меньше потребление памяти
   - Идеально для частого переобучения

2. **📊 Точность**
   - Часто показывает лучшие результаты на табличных данных
   - Лучше работает с категориальными признаками
   - Меньше переобучения

3. **🔧 Гибкость**
   - Легко интегрируется в существующий ансамбль
   - Поддерживает те же фичи, что и XGBoost
   - Совместим с текущей архитектурой

4. **💡 Дополнение к ансамблю**
   - Разные алгоритмы = разные паттерны
   - Улучшает диверсификацию предсказаний
   - Снижает риск переобучения

---

## 🏗️ АРХИТЕКТУРА

### Три варианта использования:

#### 1. **Отдельная LightGBM модель**
```python
# Обучение отдельной LightGBM модели
trainer = ModelTrainer()
lgb_model, lgb_metrics = trainer.train_lightgbm_classifier(
    X, y,
    n_estimators=100,
    max_depth=6,
    learning_rate=0.1,
)
```

#### 2. **Тройной ансамбль (RF + XGBoost + LightGBM)** ⭐ РЕКОМЕНДУЕТСЯ
```python
# Обучение тройного ансамбля
ensemble_model, ensemble_metrics = trainer.train_ensemble(
    X, y,
    ensemble_method="triple",  # Новый метод!
    include_lightgbm=True,      # Включить LightGBM
    rf_n_estimators=100,
    xgb_n_estimators=100,
    lgb_n_estimators=100,      # Параметры LightGBM
    lgb_max_depth=6,
    lgb_learning_rate=0.1,
)
```

#### 3. **Гибридный подход**
- Использовать LightGBM для быстрых предсказаний
- Использовать тройной ансамбль для важных решений
- Комбинировать результаты с существующими стратегиями

---

## 📦 УСТАНОВКА

### 1. Установить LightGBM:
```bash
pip install lightgbm>=4.0.0
```

Или обновить requirements.txt (уже добавлено):
```bash
pip install -r requirements.txt
```

### 2. Проверить установку:
```python
import lightgbm as lgb
print(f"LightGBM version: {lgb.__version__}")
```

---

## 🎓 ОБУЧЕНИЕ МОДЕЛИ

### Вариант 1: Отдельная LightGBM модель

Создайте скрипт `train_lightgbm_model.py`:

```python
from bot.ml.model_trainer import ModelTrainer
from bot.ml.feature_engineering import FeatureEngineer
from bot.ml.data_collector import DataCollector

# 1. Сбор данных
collector = DataCollector()
df = collector.collect_data("BTCUSDT", "15m", days=180)

# 2. Создание фичей
feature_engineer = FeatureEngineer()
df_with_features = feature_engineer.create_technical_indicators(df)

# 3. Подготовка таргета
df_with_target = feature_engineer.create_target(
    df_with_features,
    forward_periods=5,
    threshold_pct=1.0,
    risk_reward_ratio=1.5
)

# 4. Подготовка данных для ML
X, y = feature_engineer.prepare_features_for_ml(df_with_target)

# 5. Обучение LightGBM
trainer = ModelTrainer()
lgb_model, lgb_metrics = trainer.train_lightgbm_classifier(
    X, y,
    n_estimators=150,      # Больше деревьев для лучшей точности
    max_depth=7,           # Глубже для сложных паттернов
    learning_rate=0.05,     # Меньше learning rate для стабильности
)

# 6. Сохранение модели
trainer.save_model(
    lgb_model,
    trainer.scaler,
    feature_engineer.get_feature_names(),
    lgb_metrics,
    f"lgb_BTCUSDT_15.pkl",
    symbol="BTCUSDT",
    interval="15",
    model_type="lightgbm",
)
```

### Вариант 2: Тройной ансамбль (РЕКОМЕНДУЕТСЯ)

Обновите существующий `train_ml_model.py`:

```python
# Обучаем тройной ансамбль
ensemble_model, ensemble_metrics = trainer.train_ensemble(
    X, y,
    ensemble_method="triple",      # Новый метод!
    include_lightgbm=True,         # Включить LightGBM
    rf_n_estimators=100,
    rf_max_depth=10,
    xgb_n_estimators=100,
    xgb_max_depth=6,
    xgb_learning_rate=0.1,
    lgb_n_estimators=150,         # LightGBM параметры
    lgb_max_depth=7,
    lgb_learning_rate=0.05,
)

# Сохраняем тройной ансамбль
trainer.save_model(
    ensemble_model,
    trainer.scaler,
    feature_engineer.get_feature_names(),
    ensemble_metrics,
    f"triple_ensemble_BTCUSDT_15.pkl",
    symbol="BTCUSDT",
    interval="15",
    model_type="triple_ensemble",
)
```

---

## 🔍 СРАВНЕНИЕ С СУЩЕСТВУЮЩИМИ МОДЕЛЯМИ

### Ожидаемые преимущества:

| Метрика | RF+XGB | RF+XGB+LGB (Triple) | Улучшение |
|---------|--------|---------------------|-----------|
| **CV Accuracy** | ~0.80 | ~0.82-0.85 | +2-5% |
| **F1-Score** | ~0.80 | ~0.82-0.85 | +2-5% |
| **Скорость обучения** | 100% | 120-150% | Быстрее |
| **Память** | 100% | 80-90% | Меньше |
| **Стабильность** | Хорошая | Отличная | Лучше |

### Почему тройной ансамбль лучше:

1. **Разнообразие алгоритмов**
   - RF: случайные деревья, устойчивость к шуму
   - XGBoost: градиентный бустинг, точность
   - LightGBM: быстрый бустинг, эффективность

2. **Взвешенное голосование**
   - Веса основаны на CV метриках каждой модели
   - Лучшие модели получают больший вес
   - Автоматическая балансировка

3. **Снижение риска**
   - Если одна модель ошибается, другие компенсируют
   - Меньше ложных сигналов
   - Более стабильные предсказания

---

## 🚀 ИНТЕГРАЦИЯ В LIVE.PY

### Шаг 1: Обновить config.py

Добавить поддержку LightGBM в настройки:

```python
# В SymbolStrategySettings
enable_lightgbm_strategy: bool = False  # Новая опция

# В AppSettings
ml_model_type_for_all: Optional[str] = None  # "rf", "xgb", "lgb", "ensemble", "triple"
```

### Шаг 2: Обновить live.py

Добавить проверку типа модели:

```python
# При загрузке модели
if model_type == "lightgbm" or model_type == "triple_ensemble":
    # Модель уже поддерживается через MLStrategy
    # TripleEnsemble автоматически распознается
    pass
```

### Шаг 3: Использование

Модель автоматически работает через существующий `MLStrategy` класс:
- `build_ml_signals()` уже поддерживает все типы моделей
- `TripleEnsemble` автоматически распознается как ансамбль
- Все фильтры и логика остаются без изменений

---

## 📊 РЕКОМЕНДУЕМЫЕ ПАРАМЕТРЫ

### Для отдельной LightGBM модели:

```python
{
    "n_estimators": 150,      # Больше деревьев
    "max_depth": 7,           # Глубже для сложных паттернов
    "learning_rate": 0.05,     # Меньше для стабильности
    "num_leaves": 31,         # По умолчанию
    "feature_fraction": 0.9,  # Использовать 90% фичей
    "bagging_fraction": 0.8,  # Использовать 80% данных
    "bagging_freq": 5,        # Bagging каждые 5 итераций
}
```

### Для тройного ансамбля:

```python
{
    "rf_n_estimators": 100,
    "rf_max_depth": 10,
    "xgb_n_estimators": 100,
    "xgb_max_depth": 6,
    "xgb_learning_rate": 0.1,
    "lgb_n_estimators": 150,      # LightGBM может быть больше
    "lgb_max_depth": 7,
    "lgb_learning_rate": 0.05,
}
```

---

## 🧪 ТЕСТИРОВАНИЕ

### 1. Быстрый тест:

```python
from bot.ml.model_trainer import ModelTrainer
import numpy as np

# Создать тестовые данные
X = np.random.rand(1000, 50)
y = np.random.choice([-1, 0, 1], size=1000)

# Обучить LightGBM
trainer = ModelTrainer()
lgb_model, metrics = trainer.train_lightgbm_classifier(X, y)

print(f"Accuracy: {metrics['accuracy']:.4f}")
print(f"CV Accuracy: {metrics['cv_mean']:.4f}")
```

### 2. Тест тройного ансамбля:

```python
# Обучить тройной ансамбль
ensemble, ensemble_metrics = trainer.train_ensemble(
    X, y,
    ensemble_method="triple",
    include_lightgbm=True,
)

print(f"Triple Ensemble CV Accuracy: {ensemble_metrics['cv_mean']:.4f}")
print(f"Weights: RF={ensemble_metrics['rf_weight']:.3f}, "
      f"XGB={ensemble_metrics['xgb_weight']:.3f}, "
      f"LGB={ensemble_metrics['lgb_weight']:.3f}")
```

---

## 📈 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

### После обучения на реальных данных:

1. **Улучшение точности на 2-5%**
   - CV Accuracy: 0.80 → 0.82-0.85
   - F1-Score: 0.80 → 0.82-0.85

2. **Больше сигналов**
   - LightGBM может находить паттерны, которые пропускают RF и XGBoost
   - Увеличение количества торговых сигналов на 10-20%

3. **Лучшая стабильность**
   - Меньше ложных сигналов
   - Более согласованные предсказания
   - Меньше просадок

4. **Быстрее обучение**
   - Обучение LightGBM в 2-3 раза быстрее XGBoost
   - Чаще можно переобучать модель

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

### 1. Обучение моделей (Приоритет: ВЫСОКИЙ)
- [ ] Обучить LightGBM для BTCUSDT
- [ ] Обучить LightGBM для ETHUSDT
- [ ] Обучить LightGBM для SOLUSDT
- [ ] Обучить тройные ансамбли для всех символов

### 2. Бэктестинг (Приоритет: ВЫСОКИЙ)
- [ ] Протестировать на исторических данных (30 дней)
- [ ] Сравнить с существующими моделями
- [ ] Проанализировать метрики (Win Rate, Profit Factor)

### 3. Интеграция (Приоритет: СРЕДНИЙ)
- [ ] Добавить поддержку в config.py
- [ ] Обновить веб-интерфейс для выбора типа модели
- [ ] Добавить мониторинг LightGBM моделей

### 4. Оптимизация (Приоритет: НИЗКИЙ)
- [ ] Подобрать оптимальные параметры для каждого символа
- [ ] Экспериментировать с разными весами ансамбля
- [ ] Тестировать на разных таймфреймах

---

## 📚 ДОПОЛНИТЕЛЬНАЯ ИНФОРМАЦИЯ

### Полезные ссылки:
- [LightGBM Documentation](https://lightgbm.readthedocs.io/)
- [LightGBM vs XGBoost](https://towardsdatascience.com/lightgbm-vs-xgboost-which-algorithm-wins-the-race-1ff7dd4915f)

### Ключевые файлы:
- `bot/ml/model_trainer.py` - добавлен метод `train_lightgbm_classifier()`
- `bot/ml/model_trainer.py` - добавлен класс `TripleEnsemble`
- `requirements.txt` - добавлен `lightgbm>=4.0.0`

---

## ✅ ЧЕКЛИСТ ВНЕДРЕНИЯ

- [x] Добавлена поддержка LightGBM в `model_trainer.py`
- [x] Создан класс `TripleEnsemble` для тройного ансамбля
- [x] Обновлен `requirements.txt`
- [x] Обновлен импорт в `strategy_ml.py`
- [ ] Создан скрипт обучения LightGBM моделей
- [ ] Обучены модели для всех символов
- [ ] Проведен бэктестинг
- [ ] Интегрировано в live.py
- [ ] Добавлено в веб-интерфейс

---

## 🎉 ЗАКЛЮЧЕНИЕ

**LightGBM** - отличное дополнение к существующим ML стратегиям:
- ✅ Быстрее и эффективнее XGBoost
- ✅ Легко интегрируется в существующий код
- ✅ Улучшает точность ансамбля
- ✅ Снижает риск переобучения

**Рекомендация:** Начать с обучения тройного ансамбля (RF + XGBoost + LightGBM) для всех символов и сравнить результаты с существующими моделями.
