# ⚡ Быстрая инструкция для тестирования

## 🚀 Быстрый запуск (3 шага)

### 1. Проверка конфигурации
```bash
python check_config.py
```

### 2. Запуск веб-сервера
```bash
python start_testing.py
```
Или:
```bash
python main.py --mode web
```

### 3. Откройте в браузере
```
http://localhost:5000
```
Войдите используя логин/пароль из `.env`

---

## ✅ Что проверить в первую очередь

### В веб-интерфейсе:

1. **Вкладка "Symbols":**
   - ✅ Открывается без ошибок
   - ✅ Показывает все 3 символа (BTCUSDT, ETHUSDT, SOLUSDT)
   - ✅ Чекбоксы работают
   - ✅ Можно сохранить настройки

2. **Переключатель символов (верхняя часть):**
   - ✅ Отображается в верхней части страницы
   - ✅ Можно переключить на другой символ
   - ✅ График обновляется при переключении

3. **График:**
   - ✅ Загружается для выбранного символа
   - ✅ Показывает свечи, индикаторы, позиции, сигналы

4. **API endpoints (проверить в консоли браузера):**
   ```javascript
   // Получить активные символы
   fetch('/api/symbols/active').then(r => r.json()).then(console.log)
   
   // Получить статус
   fetch('/api/status').then(r => r.json()).then(console.log)
   
   // Получить график для BTCUSDT
   fetch('/api/chart/data?symbol=BTCUSDT').then(r => r.json()).then(console.log)
   ```

5. **Запуск бота:**
   - ✅ Кнопка "Start Bot" работает
   - ✅ В логах видно запуск воркеров для каждого символа
   - ✅ Статус изменяется на "Running"

---

## 📝 Настройка активных символов

### Через веб-интерфейс:
1. Откройте вкладку **"Symbols"**
2. Выберите нужные символы чекбоксами
3. Выберите PRIMARY_SYMBOL
4. Нажмите **"Save Symbol Settings"**

### Через .env файл:
```env
ACTIVE_SYMBOLS=BTCUSDT,ETHUSDT,SOLUSDT
PRIMARY_SYMBOL=BTCUSDT
```

### Через API (требует авторизации):
```bash
curl -X POST http://localhost:5000/api/symbols/set-active \
  -H "Content-Type: application/json" \
  -d '{"symbols": ["BTCUSDT", "ETHUSDT"], "primary": "BTCUSDT"}'
```

---

## 🔍 Что проверить в логах

При запуске должны появиться:
```
[web] MultiSymbolManager initialized with active symbols: ['BTCUSDT', 'ETHUSDT', 'SOLUSDT']
```

При запуске бота:
```
[MultiSymbol] ✅ Started worker thread for BTCUSDT
[MultiSymbol] ✅ Started worker thread for ETHUSDT
[live] [BTCUSDT] Starting live trading bot
[live] [ETHUSDT] Starting live trading bot
```

При работе:
```
[live] [BTCUSDT] 🔍 Checking for existing open positions...
[live] [ETHUSDT] 📊 TREND strategy: generated X signals
```

---

## ❌ Возможные проблемы

### Проблема: Веб-сервер не запускается
**Решение:**
- Проверьте, что все зависимости установлены: `pip install -r requirements.txt`
- Проверьте, что порт 5000 свободен
- Попробуйте другой порт: `python main.py --mode web --web-port 5001`

### Проблема: Ошибка "MultiSymbolManager not initialized"
**Решение:**
- Убедитесь, что `init_app()` вызывается при запуске
- Проверьте логи - должно быть сообщение об инициализации

### Проблема: График не загружается
**Решение:**
- Проверьте консоль браузера на наличие ошибок JavaScript
- Убедитесь, что API endpoint `/api/chart/data?symbol=...` доступен
- Проверьте, что символ валидный (BTCUSDT, ETHUSDT, SOLUSDT)

### Проблема: Настройки символов не сохраняются
**Решение:**
- Проверьте права на запись в `.env` файл
- Проверьте логи - должно быть сообщение о сохранении
- Убедитесь, что функция `_save_symbol_settings_to_env()` вызывается

---

## ✅ Чек-лист успешного тестирования

- [ ] Конфигурация загружается без ошибок
- [ ] Веб-сервер запускается без ошибок
- [ ] Вход в админку работает
- [ ] Вкладка "Symbols" отображается и работает
- [ ] Переключатель символов работает
- [ ] График загружается для каждого символа
- [ ] API endpoints работают
- [ ] Бот запускается для одного символа
- [ ] Бот запускается для нескольких символов
- [ ] Логи показывают работу всех воркеров
- [ ] Настройки символов сохраняются
- [ ] Переключение основного символа работает

---

## 📚 Дополнительная документация

- `TEST_REPORT.md` - Подробный отчет о тестировании
- `REAL_TESTING_GUIDE.md` - Детальные сценарии тестирования
- `START_TESTING.md` - Полная инструкция по запуску

---

**Готово к тестированию! 🚀**
