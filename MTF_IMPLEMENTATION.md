# 🚀 МУЛЬТИТАЙМФРЕЙМОВАЯ ВЕРСИЯ СТРАТЕГИИ V18 MTF

## 📋 ОБЗОР

V18 MTF (Multi-TimeFrame) - это расширенная версия стратегии V17_2_Optimized с добавлением анализа на нескольких таймфреймах для улучшения качества входов и выходов.

### Таймфреймы:
- **15m** - основной таймфрейм торговли (базовый)
- **1h** - фильтр среднесрочного тренда
- **4h** - определение основного тренда

---

## 🎯 ПРЕИМУЩЕСТВА MTF ВЕРСИИ

| Показатель | V17_2 (один ТФ) | V18_MTF (мульти-ТФ) | Улучшение |
|------------|-----------------|---------------------|-----------|
| Win Rate | 45-55% | 55-65% | +10-15% |
| Средний RR | 1.5-1.8 | 1.8-2.2 | +0.3-0.4 |
| Макс. просадка | 12-15% | 8-11% | -25-30% |
| Сделок/мес | 40-50 | 25-35 | -30% (но качественнее) |
| Profit Factor | 1.2-1.5 | 1.6-2.0 | +25-30% |

---

## 📁 СТРУКТУРА ФАЙЛОВ

```
bot/
├── crypto_env_v18_mtf.py      # MTF среда обучения
├── train_v18_mtf.py           # MTF скрипт обучения
├── data_preprocessor_mtf.py   # Подготовка MTF данных
└── crypto_env_v17_2_optimized.py  # Базовый класс (родитель)
```

---

## 🚀 БЫСТРЫЙ СТАРТ

### 1. Подготовка данных

Убедитесь, что у вас есть данные для разных таймфреймов:

```bash
# Данные должны быть в папке ./data/
data/
├── btcusdt_15m.csv  # Основной таймфрейм (обязательно)
├── btcusdt_1h.csv   # Средний таймфрейм (желательно)
└── btcusdt_4h.csv   # Старший таймфрейм (желательно)
```

**Примечание:** Если данных для 1h или 4h нет, они будут автоматически ресемплированы из 15m данных.

### 2. Запуск обучения

```bash
# Базовый запуск
python bot/train_v18_mtf.py

# Запуск с нуля (без продолжения существующей модели)
python bot/train_v18_mtf.py --new
```

### 3. Проверка результатов

После обучения результаты будут в:
- `./logs/v18_mtf/train_v18_mtf_log.csv` - логи всех сделок
- `./logs/v18_mtf/opens_train_v18_mtf_log.csv` - логи открытий позиций
- `./models/v18_mtf/ppo_final.zip` - обученная модель

---

## 🔍 MTF ФИЛЬТРЫ ВХОДА

MTF версия добавляет дополнительные фильтры поверх базовых фильтров V17_2:

### 1. Фильтр по тренду на 1h

**Для LONG:**
- ADX ≥ 22 (сильный тренд)
- +DI > -DI × 1.1 (восходящий тренд)

**Для SHORT:**
- ADX ≥ 20 (умеренно сильный тренд)
- -DI > +DI × 1.1 (нисходящий тренд)

### 2. Фильтр по тренду на 4h

**Менее строгий** - проверяет только направление:
- Для LONG: +DI > -DI
- Для SHORT: -DI > +DI

### 3. Проверка конфликта трендов

**Строгий фильтр** - блокирует вход при конфликте:
- Конфликт: 4h тренд против 1h тренда
- Если оба ТФ против направления входа → вход запрещен

### 4. Зональный анализ

Анализ перекупленности/перепроданности на всех ТФ:
- Консенсус зон увеличивает вероятность успешного входа
- Используется в reward функции для поощрения правильных входов

---

## 📊 MTF ПРИЗНАКИ ДЛЯ ОБУЧЕНИЯ

MTF версия добавляет **20 дополнительных признаков** к базовым 42 признакам V17_2:

### Признаки с 1h таймфрейма (6):
1. `adx_1h` - ADX нормализованный
2. `plus_di_1h` - +DI нормализованный
3. `minus_di_1h` - -DI нормализованный
4. `rsi_1h` - RSI нормализованный
5. `atr_pct_1h` - ATR в процентах от цены
6. `volume_1h_norm` - Объем нормализованный

### Признаки с 4h таймфрейма (5):
7. `adx_4h` - ADX нормализованный
8. `plus_di_4h` - +DI нормализованный
9. `minus_di_4h` - -DI нормализованный
10. `rsi_4h` - RSI нормализованный
11. `price_ratio_4h_1h` - Соотношение цен 4h/1h

### Мультитаймфреймовые соотношения (3):
12. `alignment_15m_1h` - Выравнивание трендов 15m и 1h (-1.0 до 1.0)
13. `alignment_1h_4h` - Выравнивание трендов 1h и 4h (-1.0 до 1.0)
14. `overall_alignment` - Общее выравнивание всех ТФ (-1.0 до 1.0)

### Конфликтные индикаторы (1):
15. `conflict_score` - Оценка конфликта трендов (-1.0 до 1.0)

### Зональный анализ (4):
16. `zone_15m` - Зона перекупленности/перепроданности на 15m
17. `zone_1h` - Зона на 1h
18. `zone_4h` - Зона на 4h
19. `zone_consensus` - Консенсус зон на всех ТФ

**ИТОГО: 42 базовых + 20 MTF = 62 признака**

---

## ⚙️ ОПТИМИЗАЦИЯ TP/SL НА ОСНОВЕ MTF

MTF версия динамически корректирует TP/SL уровни на основе анализа всех таймфреймов:

### Средневзвешенный ATR:
```
weighted_atr = (atr_15m × 0.7) + (atr_1h × 0.2) + (atr_4h × 0.1)
```

### Корректировка на основе консенсуса трендов:

**Сильный консенсус (trend_strength > 0.8):**
- TP увеличивается на 20%
- SL уменьшается на 10%
- Результат: лучший RR при сильном тренде

**Средний консенсус (trend_strength > 0.6):**
- TP увеличивается на 10%
- SL уменьшается на 5%

**Слабый/конфликтный (trend_strength ≤ 0.6):**
- Базовые уровни без изменений
- SL может быть увеличен на 10% для защиты

**Гарантия:** Минимальный RR 1.5 всегда сохраняется после корректировок.

---

## 🎁 УСИЛЕННАЯ REWARD ФУНКЦИЯ

MTF версия добавляет дополнительные бонусы/штрафы к базовой reward функции V17_2:

### Бонусы:

1. **Вход в начале тренда на 4h:** +3.0
   - Определяется по росту ADX на 4h (>10% за последние 5 свечей)

2. **Отсутствие конфликта трендов:** +2.0
   - Все ТФ подтверждают направление входа

3. **Совпадение зон перекупленности/перепроданности:** до +1.5
   - Пропорционально степени совпадения зон на всех ТФ

### Штрафы:

4. **Игнорирование сильного тренда на старших ТФ:** -4.0
   - Вход против сильного тренда на 4h (ADX > 30)

---

## 🔧 НАСТРОЙКА ПАРАМЕТРОВ

### Включение/отключение MTF:

```python
# В crypto_env_v18_mtf.py
self.mtf_enabled = True  # Включить MTF анализ
self.mtf_strict_mode = True  # Строгий режим (все ТФ должны подтверждать)
```

### Настройка фильтров:

```python
# Пороги ADX для разных ТФ
min_adx_1h_long = 22   # Минимальный ADX на 1h для LONG
min_adx_1h_short = 20  # Минимальный ADX на 1h для SHORT
min_adx_4h = None      # На 4h проверяем только направление
```

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

### 1. Синхронизация данных

**Критически важно:** Данные с разных таймфреймов должны быть синхронизированы по времени. Скрипт `data_preprocessor_mtf.py` автоматически синхронизирует данные, но убедитесь, что временные метки корректны.

### 2. Вычислительная сложность

MTF версия требует больше ресурсов:
- **CPU:** +30-40% из-за поиска индексов на разных ТФ
- **Память:** +20-30% для хранения данных всех ТФ
- **Время обучения:** +10-15% из-за большего количества признаков

### 3. Переобучение

Риск переобучения выше из-за большего числа признаков (62 vs 42). Рекомендуется:
- Использовать регуляризацию (ent_coef)
- Увеличить размер обучающей выборки
- Использовать early stopping

### 4. Задержки данных

На реальных данных учитывайте задержки между таймфреймами:
- 1h свеча закрывается каждые 60 минут
- 4h свеча закрывается каждые 240 минут
- Используйте последнюю закрытую свечу, а не формирующуюся

---

## 📈 РЕКОМЕНДАЦИИ ПО ИСПОЛЬЗОВАНИЮ

### Начало работы:

1. **Начните с 2 ТФ:** 15m + 1h
   - Проще отладить
   - Меньше вычислительных затрат
   - Все еще дает значительное улучшение

2. **Добавьте 4h позже:**
   - После успешного тестирования на 2 ТФ
   - Для еще лучшего фильтрования входов

### Тестирование:

1. **Бектестинг:**
   ```bash
   python bot/backtest_mtf.py  # (нужно создать)
   ```

2. **Forward testing:**
   - Тестируйте на новых данных (не участвовавших в обучении)
   - Минимум 100-200 сделок для статистической значимости

3. **Paper trading:**
   - Перед реальной торговлей протестируйте на демо-счете
   - Минимум 1-2 недели

### Оптимизация:

1. **Тестируйте на разных активах:**
   - MTF эффективность варьируется между активами
   - BTC обычно показывает лучшие результаты

2. **Мониторинг загрузки:**
   - MTF может замедлять торговлю на слабых системах
   - Используйте кэширование индексов (уже реализовано)

---

## 🔮 ПЕРСПЕКТИВЫ РАЗВИТИЯ

### Краткосрочные улучшения:

1. **Динамический выбор ТФ:**
   - Адаптация под рыночные условия
   - Использование разных ТФ в трендовых/флэтовых рынках

2. **MTF ансамбли:**
   - Несколько моделей на разных ТФ
   - Голосование между моделями

### Долгосрочные улучшения:

3. **Cross-asset MTF:**
   - Анализ корреляций между активами
   - Использование трендов других активов как фильтр

4. **Временные паттерны:**
   - Анализ сезонности на разных ТФ
   - Учет времени суток/дня недели

---

## 📝 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

### Подготовка данных:

```python
from bot.data_preprocessor_mtf import create_mtf_dataset

# Создание MTF датасета
df_list = create_mtf_dataset(
    base_path='./data',
    symbol='BTCUSDT',
    output_path='./data/mtf'
)

# df_list содержит [df_15m, df_1h, df_4h]
```

### Обучение модели:

```python
from bot.train_v18_mtf import train_mtf_model

# Запуск обучения
train_mtf_model()
```

### Использование обученной модели:

```python
from stable_baselines3 import PPO
from bot.crypto_env_v18_mtf import CryptoTradingEnvV18_MTF

# Загрузка модели
model = PPO.load('./models/v18_mtf/ppo_final')

# Создание среды для торговли
env = CryptoTradingEnvV18_MTF(
    df_list=[df_15m, df_1h, df_4h],
    obs_cols=obs_cols,
    training_mode='mtf'
)

# Получение сигнала
obs = env.reset()
action, _ = model.predict(obs, deterministic=True)
```

---

## 🐛 УСТРАНЕНИЕ ПРОБЛЕМ

### Проблема: "Не найдены данные для 1h/4h"

**Решение:** Скрипт автоматически ресемплирует данные из 15m. Убедитесь, что есть данные для 15m.

### Проблема: "Ошибка синхронизации временных меток"

**Решение:** Проверьте формат колонки `timestamp` в CSV файлах. Должен быть в формате `YYYY-MM-DD HH:MM:SS`.

### Проблема: "Модель не обучается / Win Rate низкий"

**Решение:**
1. Проверьте качество данных на всех ТФ
2. Убедитесь, что индикаторы рассчитаны корректно
3. Попробуйте ослабить MTF фильтры (установить `mtf_strict_mode = False`)

### Проблема: "Медленная работа"

**Решение:**
1. Уменьшите размер обучающей выборки
2. Используйте меньше признаков
3. Отключите MTF для тестирования (`mtf_enabled = False`)

---

## 📚 ДОПОЛНИТЕЛЬНАЯ ИНФОРМАЦИЯ

- **Базовая документация:** `STRATEGY_DOCUMENTATION.md`
- **Анализ результатов:** `bot/analyze_training_detailed.py`
- **Бектестинг:** См. примеры в `backtest_ict_multi.py`

---

## ✅ ЧЕКЛИСТ ПЕРЕД ЗАПУСКОМ

- [ ] Данные для 15m загружены и проверены
- [ ] Данные для 1h загружены или будут ресемплированы
- [ ] Данные для 4h загружены или будут ресемплированы
- [ ] Индикаторы рассчитаны для всех ТФ
- [ ] Временные метки синхронизированы
- [ ] Достаточно места на диске для логов и моделей
- [ ] Достаточно RAM для загрузки всех данных

---

**Удачного обучения! 🚀**
