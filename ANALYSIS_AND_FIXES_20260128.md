# Анализ результатов тестирования после оптимизации порогов

**Дата:** 2026-01-28  
**Файл:** `ml_models_comparison_20260128_225129.csv`

---

## 🚨 КРИТИЧЕСКИЕ ПРОБЛЕМЫ

### 1. КРИТИЧЕСКИ МАЛО СИГНАЛОВ
- **Текущее:** 1-2 сделки за 30 дней
- **Целевое:** 1-10 сигналов в день (30-300 за 30 дней)
- **Проблема:** Пороги СЛИШКОМ ВЫСОКИЕ

### 2. Win Rate = 0%
- Все сделки убыточные
- Все закрываются по SL или таймауту
- Profit Factor = 0.0

### 3. ВСЕ СИГНАЛЫ SHORT
- `long_trades = 0` для всех моделей
- Это указывает на проблему с логикой или данными

### 4. ВСЕ СДЕЛКИ ЗАКРЫВАЮТСЯ ПО ТАЙМАУТУ
- `avg_trade_duration_hours = 168.0` (7 дней)
- Это максимальное время удержания позиции
- Ни одна сделка не достигла ни TP, ни SL

**✅ Найдена корневая причина (фикc):**
- В `MLStrategy` значения `tp_pct/sl_pct` формируются как **проценты** (например `1.80` означает `1.80%` от цены).
- В `backtest_ml_strategy.py` эти значения ошибочно применялись как **доли** (использовалось `1 - sl_pct` вместо `1 - sl_pct/100`),
  из‑за чего TP/SL становились почти недостижимыми и сделки закрывались по таймауту `168h`.

---

## 🔍 ПРИЧИНЫ

### Пороги слишком высокие:
- `threshold_mult = 0.25-0.35` (25-35% от стандартного) - СЛИШКОМ ВЫСОКО
- `min_strength = 0.005-0.007` (0.5-0.7%) - СЛИШКОМ ВЫСОКО
- `dynamic_threshold = 0.30` (30%) - СЛИШКОМ ВЫСОКО

### Возможные проблемы с TP/SL:
- TP: 20% от маржи = 2.0% от цены (при 10x)
- SL: 8% от маржи = 0.8% от цены (при 10x)
- Возможно, TP слишком далеко, а SL слишком близко

---

## ✅ РЕШЕНИЯ

### 1. СНИЗИТЬ ПОРОГИ УВЕРЕННОСТИ

**Текущие значения:**
```python
threshold_mult = 0.25-0.35  # 25-35%
dynamic_threshold = 0.30    # 30%
min_strength = 0.005-0.007  # 0.5-0.7%
```

**Новые значения (более агрессивные):**
```python
threshold_mult = 0.15-0.20  # 15-20% (снижено на 40%)
dynamic_threshold = 0.20    # 20% (снижено на 33%)
min_strength = 0.003-0.004  # 0.3-0.4% (снижено на 40%)
```

### 2. ПРОВЕРИТЬ TP/SL НАСТРОЙКИ

Возможно, нужно:
- Уменьшить TP до 1.5-1.8% от цены (было 2.0%)
- Увеличить SL до 1.0% от цены (было 0.8%)
- Это даст более реалистичное соотношение RR = 1.5-1.8:1

### 3. ПРОВЕРИТЬ ЛОГИКУ LONG СИГНАЛОВ

Нужно убедиться, что LONG сигналы генерируются корректно.
