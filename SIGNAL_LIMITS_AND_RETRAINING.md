# Ограничение сигналов в день и переобучение моделей

**Дата:** 2026-01-28

---

## ✅ РЕАЛИЗОВАНО: Настройка порогов для получения 1-10 сигналов в день

### Настройки (добавлены в `bot/config.py`):
- `ml_min_signals_per_day: int = 1` - Целевой минимум сигналов в день (для статистики)
- `ml_max_signals_per_day: int = 10` - Целевой максимум сигналов в день (для статистики)

**ВАЖНО:** Это не жесткие ограничения, а целевые показатели для оценки качества стратегии!

### Логика работы:

1. **Отслеживание сигналов (для статистики):**
   - Каждый день отслеживается количество сгенерированных сигналов (LONG/SHORT)
   - Счетчик обновляется при генерации каждого сигнала
   - Старые даты (старше 7 дней) автоматически очищаются
   - Используется для оценки работы стратегии и статистики

2. **Естественная фильтрация через пороги уверенности:**
   - Пороги настроены так, чтобы естественным образом получать 1-10 качественных сигналов в день
   - Для ансамблей: пороги 25-35% от стандартного (умеренные)
   - Минимальная сила сигнала: 0.5-0.7% (умеренные требования)
   - Фильтрация происходит через качество сигналов, а не через жесткие лимиты

3. **Защита от ошибок:**
   - Только защита от явных ошибок (100+ сигналов в день)
   - Это защита от багов, а не ограничение нормальной работы

### Преимущества:
- ✅ Естественная генерация 1-10 качественных сигналов в день
- ✅ Нет жестких ограничений, которые могут блокировать хорошие сигналы
- ✅ Статистика для оценки работы стратегии
- ✅ Пороги настроены для оптимального баланса между количеством и качеством

---

## 📚 ПЕРЕОБУЧЕНИЕ МОДЕЛЕЙ: Нужно ли?

### ❌ **НЕТ, переобучение НЕ требуется**

#### Почему:

1. **Мы изменили только фильтры сигналов, а не модели:**
   - Изменены пороги уверенности в коде стратегии (`bot/ml/strategy_ml.py`)
   - Изменены TP/SL настройки в конфигурации (`bot/config.py`)
   - **Сами модели остались без изменений** - они обучены на исторических данных и продолжают работать корректно

2. **Модели предсказывают направление движения, а не количество сигналов:**
   - Модели обучены предсказывать: LONG, SHORT или HOLD
   - Они не знают о лимитах сигналов в день
   - Фильтры сигналов применяются **после** предсказания модели

3. **Изменения порогов не влияют на качество предсказаний:**
   - Снижение порогов уверенности просто позволяет принимать больше сигналов
   - Модель все еще делает те же предсказания с той же точностью
   - Мы просто стали более "либеральными" в принятии решений

### Когда переобучение МОЖЕТ быть полезно:

1. **Если изменились рыночные условия:**
   - Рынок стал более волатильным
   - Изменились паттерны движения цены
   - Прошло много времени с момента обучения (более 3-6 месяцев)

2. **Если хотите улучшить качество предсказаний:**
   - Модель показывает низкую точность
   - Win Rate ниже ожидаемого
   - Profit Factor отрицательный

3. **Если изменились фичи или целевая переменная:**
   - Добавлены новые индикаторы
   - Изменен способ расчета целевой переменной
   - Изменен `min_risk_reward_ratio` при обучении

### Текущая ситуация:

- ✅ Модели обучены с `min_risk_reward_ratio=2.0` (соответствует нашим ожиданиям)
- ✅ Используются актуальные фичи (включая MTF для некоторых моделей)
- ✅ Изменения касаются только фильтрации сигналов, а не обучения

**Вывод:** Переобучение моделей **НЕ требуется** на данном этапе. Можно продолжать использовать существующие модели.

---

## 🔄 РЕКОМЕНДАЦИИ ПО МОНИТОРИНГУ

### Через 1-2 недели после внедрения:

1. **Проверить количество сигналов:**
   - Убедиться, что генерируется 1-10 сигналов в день
   - Если сигналов слишком мало (< 1 в день) - снизить пороги еще больше
   - Если сигналов слишком много (> 10 в день) - увеличить пороги

2. **Оценить качество сигналов:**
   - Запустить `compare_ml_models.py` для сравнения результатов
   - Проверить Win Rate и Profit Factor
   - Если результаты хуже ожидаемых - рассмотреть переобучение

3. **Оптимизировать параметры:**
   - Настроить `ml_min_signals_per_day` и `ml_max_signals_per_day` под свои нужды
   - Возможно, изменить пороги уверенности для ансамблей

---

## 📝 ИТОГО

- ✅ **Ограничение сигналов реализовано:** 1-10 сигналов в день
- ❌ **Переобучение НЕ требуется:** модели работают корректно
- 🔄 **Мониторинг:** проверять результаты через 1-2 недели
