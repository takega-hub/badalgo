# Корректировка фильтров таргета ML

**Дата:** 2026-01-29  
**Проблема:** Новые фильтры слишком строгие - MTF модели дают 0 сделок

---

## 🔍 ПРОБЛЕМА

После применения новых фильтров:
- **MTF модели:** 0 сделок (все отфильтрованы)
- **15m модели:** Те же результаты (модели не переобучены)

**Причина:** Фильтры `min_profit_pct=1.5%` и `max_hold_periods=32` (8 часов) слишком строгие и отфильтровали все примеры для обучения.

---

## ✅ РЕШЕНИЕ

### 1. Смягчены параметры фильтрации

**Было:**
- `min_profit_pct = 1.5%` - слишком строго
- `max_hold_periods = 32` (8 часов) - слишком коротко

**Стало:**
- `min_profit_pct = 1.0%` - смягчено на 33%
- `max_hold_periods = 48` (12 часов) - увеличено на 50%

### 2. Добавлена fallback логика

Если после фильтрации осталось слишком мало примеров (< 0.5% данных или < 100 примеров):
- Автоматически смягчаются условия:
  - RR: с 2.0 до 1.4 (70% от исходного)
  - Минимальная прибыль: с 1.0% до 0.6% (60% от исходного)
  - Убирается проверка времени достижения цели

### 3. Умная проверка времени

Фильтр по времени (`max_hold_periods`) применяется только если:
- Есть достаточно примеров после базовой фильтрации (>= 1% данных или >= 100 примеров)
- Иначе фильтр времени не применяется, чтобы не отфильтровать все примеры

---

## 📊 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

После смягчения фильтров:
- ✅ **Больше примеров для обучения** - модели смогут обучиться
- ✅ **MTF модели** - должны давать сигналы (не 0)
- ✅ **Качество сохраняется** - фильтры все еще строгие, но не слишком

---

## 🚀 СЛЕДУЮЩИЕ ШАГИ

1. **Переобучить модели** с новыми параметрами:
   ```bash
   python retrain_all_models.py
   ```

2. **Протестировать:**
   ```bash
   python compare_ml_models.py --days 30 --symbols BTCUSDT
   ```

3. **Оценить результаты:**
   - MTF модели должны давать > 0 сделок
   - WinRate должен быть > 30%
   - Profit Factor должен быть > 0.8

---

## 📝 ИЗМЕНЕНИЯ В КОДЕ

### `bot/ml/feature_engineering.py`
- Смягчены параметры по умолчанию
- Добавлена fallback логика при недостатке примеров
- Умная проверка времени (применяется только если есть достаточно примеров)

### Все обучающие скрипты
- Обновлены параметры `max_hold_periods=48` и `min_profit_pct=1.0`

---

**Готово к переобучению!** 🚀
