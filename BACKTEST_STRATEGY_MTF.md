# 📊 Бэктест стратегий с мультитаймфреймовым анализом

## Описание

Скрипт `backtest_strategy_mtf.py` тестирует стратегии из `bot/strategy.py` (TREND, FLAT, MOMENTUM) на исторических данных с расчетом винрейта, PnL и других метрик.

## Новые возможности

### ✅ Мультитаймфреймовый анализ (MTF)

Добавлена функция `_get_higher_timeframe_bias()` в `bot/strategy.py`, которая:
- Ресемплит данные на высший таймфрейм (1H или 4H)
- Рассчитывает EMA на высшем таймфрейме
- Определяет глобальный тренд (bullish/bearish)
- Фильтрует сигналы: LONG разрешен только при bullish тренде, SHORT - только при bearish

### 📈 Метрики бэктеста

- **Win Rate** - процент прибыльных сделок
- **Total PnL** - общая прибыль/убыток в USD и %
- **Profit Factor** - отношение прибыли к убыткам
- **Max Drawdown** - максимальная просадка
- **Sharpe Ratio** - коэффициент Шарпа (годовой)
- **Average Trade Duration** - средняя длительность сделки

## Использование

### Базовый запуск

```bash
python backtest_strategy_mtf.py --csv data/btcusdt_15m.csv --strategy trend
```

### С мультитаймфреймовым фильтром (используя все таймфреймы)

```bash
# С MTF консенсусом (автоматически использует 1H и 4H данные из папки data/)
python backtest_strategy_mtf.py --csv data/btcusdt_15m.csv --strategy trend --mtf

# Только с одним таймфреймом (без использования готовых данных)
python backtest_strategy_mtf.py --csv data/btcusdt_15m.csv --strategy trend --mtf --mtf-tf 1h --no-all-tf
```

### Параметры

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `--csv` | Путь к CSV файлу с историческими данными | `data/btcusdt_15m.csv` |
| `--strategy` | Тип стратегии: `trend`, `flat`, `momentum` | `trend` |
| `--mtf` | Использовать мультитаймфреймовый фильтр | `False` |
| `--mtf-tf` | Таймфрейм для MTF: `1h` или `4h` (если не используются все таймфреймы) | `1h` |
| `--no-all-tf` | Не использовать все таймфреймы, только ресемплинг из 15m | `False` |
| `--balance` | Начальный баланс | `1000.0` |
| `--symbol` | Торговая пара (для логирования) | `BTCUSDT` |

### Примеры

```bash
# Тест TREND стратегии без MTF
python backtest_strategy_mtf.py --csv data/btcusdt_15m.csv --strategy trend

# Тест TREND стратегии с MTF консенсусом (использует 1H + 4H данные)
python backtest_strategy_mtf.py --csv data/btcusdt_15m.csv --strategy trend --mtf

# Тест TREND стратегии с MTF фильтром только 1H (без готовых данных)
python backtest_strategy_mtf.py --csv data/btcusdt_15m.csv --strategy trend --mtf --mtf-tf 1h --no-all-tf

# Тест FLAT стратегии с MTF консенсусом
python backtest_strategy_mtf.py --csv data/ethusdt_15m.csv --strategy flat --mtf --symbol ETHUSDT

# Тест MOMENTUM стратегии с MTF консенсусом
python backtest_strategy_mtf.py --csv data/solusdt_15m.csv --strategy momentum --mtf --symbol SOLUSDT
```

## Формат CSV файла

CSV файл должен содержать колонки:
- `datetime` или `timestamp` - временная метка
- `open`, `high`, `low`, `close` - OHLC данные
- `volume` - объем

Пример:
```csv
datetime,open,high,low,close,volume
2026-01-01 00:00:00,45000,45200,44900,45100,1000.5
2026-01-01 00:15:00,45100,45300,45000,45200,1200.3
```

## Результаты

### Консольный вывод

```
================================================================================
📊 BACKTEST: TREND Strategy with MTF
================================================================================

📁 Loading data from data/btcusdt_15m.csv...
   Loaded 1000 candles
   Date range: 2026-01-01 to 2026-01-15

🔧 Preparing indicators...

📈 Generating TREND signals...
   Using MTF filter: 1h timeframe
   Generated 45 actionable signals
   LONG: 23
   SHORT: 22

💰 Running simulator (Initial balance: $1000.00)...

================================================================================
📊 BACKTEST RESULTS
================================================================================

📈 Signals:
   Total signals: 45
   LONG signals: 23
   SHORT signals: 22

💼 Trades:
   Total trades: 30
   Winning trades: 20
   Losing trades: 10
   Win Rate: 66.67%

💰 PnL:
   Total PnL: $125.50 (+12.55%)
   Average Win: $15.25
   Average Loss: -$8.50
   Profit Factor: 3.59

📉 Risk Metrics:
   Max Drawdown: $45.20 (4.52%)
   Sharpe Ratio: 1.85
   Avg Trade Duration: 4.2 hours
```

### CSV файл с результатами

Результаты сохраняются в файл:
```
backtest_trend_BTCUSDT_mtf.csv
```

Содержит колонки:
- `entry_time`, `exit_time` - время входа/выхода
- `side` - направление (LONG/SHORT)
- `entry_price`, `exit_price` - цены
- `size_usd` - размер позиции
- `pnl`, `pnl_pct` - прибыль/убыток
- `entry_reason`, `exit_reason` - причины входа/выхода

## Сравнение с/без MTF фильтра

Для сравнения эффективности MTF фильтра запустите два теста:

```bash
# Без MTF
python backtest_strategy_mtf.py --csv data/btcusdt_15m.csv --strategy trend > results_no_mtf.txt

# С MTF
python backtest_strategy_mtf.py --csv data/btcusdt_15m.csv --strategy trend --mtf > results_with_mtf.txt
```

Сравните метрики:
- **Win Rate** - должен увеличиться с MTF
- **Total PnL** - может быть выше с MTF за счет фильтрации плохих сигналов
- **Total Trades** - может быть меньше с MTF (меньше сигналов, но качественнее)

## Технические детали

### Мультитаймфреймовый анализ

#### Использование всех таймфреймов (рекомендуется)

По умолчанию тест использует готовые данные со всех таймфреймов из папки `data/`:
- `{symbol}_15m.csv` - основной таймфрейм для торговли
- `{symbol}_1h.csv` - для анализа среднесрочного тренда
- `{symbol}_4h.csv` - для анализа долгосрочного тренда

Функция `_get_multi_timeframe_consensus()`:
1. Загружает данные с 1H и 4H таймфреймов (если доступны)
2. Рассчитывает EMA(50) на каждом таймфрейме
3. Определяет тренд на каждом таймфрейме:
   - `bullish` - цена выше EMA
   - `bearish` - цена ниже EMA
   - `None` - недостаточно данных
4. Формирует консенсус:
   - **Сильный bullish** - оба таймфрейма bullish (trend_strength = 1.0)
   - **Сильный bearish** - оба таймфрейма bearish (trend_strength = 1.0)
   - **Neutral** - противоречивые сигналы (trend_strength = 0.3)
   - **Слабый** - только один таймфрейм дал результат (trend_strength = 0.5)

#### Ресемплинг (fallback)

Если готовые данные недоступны, используется ресемплинг из 15m данных:
- Функция `_get_higher_timeframe_bias()` ресемплит 15m → 1H или 4H
- Менее точный метод, но работает если нет готовых файлов

### Фильтрация сигналов

- **LONG сигналы** блокируются если MTF консенсус = `bearish`
- **SHORT сигналы** блокируются если MTF консенсус = `bullish`
- **Neutral консенсус** - не блокирует сигналы (рынок неопределенный)
- Если MTF консенсус = `None` (недостаточно данных), фильтр не применяется

### Преимущества использования всех таймфреймов

1. **Точность** - используются реальные свечи с биржи, а не ресемплинг
2. **Консенсус** - анализ общей ситуации на рынке по всем таймфреймам
3. **Надежность** - меньше ложных сигналов за счет фильтрации противоречивых трендов

## Требования

- Python 3.8+
- pandas
- numpy
- bot.config, bot.strategy, bot.indicators, bot.simulation модули

## Примечания

- MTF фильтр требует достаточно исторических данных (минимум 50-100 свечей на высшем таймфрейме)
- Для 1H фильтра нужно минимум ~100 свечей 15m данных
- Для 4H фильтра нужно минимум ~400 свечей 15m данных
- Если данных недостаточно, фильтр не применяется (не блокирует сигналы)
