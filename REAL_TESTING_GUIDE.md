# Руководство по реальному тестированию многопарной торговли

## Подготовка к тестированию

### 1. Проверка конфигурации

Запустите скрипт проверки:
```bash
python check_config.py
```

Этот скрипт проверит:
- ✅ Наличие необходимых настроек в .env
- ✅ Настройки символов (ACTIVE_SYMBOLS, PRIMARY_SYMBOL)
- ✅ Наличие ML моделей для всех символов
- ✅ Загрузку конфигурации
- ✅ Инициализацию MultiSymbolManager

### 2. Настройка .env файла

Убедитесь, что в `.env` файле установлены:
```env
# Multi-Symbol Trading
ACTIVE_SYMBOLS=BTCUSDT,ETHUSDT,SOLUSDT
PRIMARY_SYMBOL=BTCUSDT

# Или для начала с одним символом:
ACTIVE_SYMBOLS=BTCUSDT
PRIMARY_SYMBOL=BTCUSDT
```

---

## Сценарии тестирования

### Сценарий 1: Тестирование с одним символом

**Цель:** Убедиться, что базовая функциональность работает с одним активным символом.

**Шаги:**
1. Установите в `.env`:
   ```env
   ACTIVE_SYMBOLS=BTCUSDT
   PRIMARY_SYMBOL=BTCUSDT
   ```

2. Запустите веб-сервер:
   ```bash
   python main.py --mode web
   ```
   Или:
   ```bash
   python -m bot.web.app
   ```

3. Откройте браузер и перейдите на `http://localhost:5000`

4. **Проверьте:**
   - ✅ Вход в админку работает
   - ✅ Вкладка "Symbols" отображается
   - ✅ Переключатель символов показывает BTCUSDT
   - ✅ Статус бота загружается
   - ✅ График загружается для BTCUSDT
   - ✅ Запуск/остановка бота работает

5. **Проверьте API endpoints:**
   ```bash
   # Получить список символов
   curl http://localhost:5000/api/symbols/list
   
   # Получить активные символы
   curl http://localhost:5000/api/symbols/active
   
   # Получить статус
   curl http://localhost:5000/api/status
   
   # Получить данные графика для BTCUSDT
   curl http://localhost:5000/api/chart/data?symbol=BTCUSDT
   ```

---

### Сценарий 2: Тестирование с несколькими символами

**Цель:** Убедиться, что бот может работать с несколькими символами одновременно.

**Шаги:**
1. Установите в `.env`:
   ```env
   ACTIVE_SYMBOLS=BTCUSDT,ETHUSDT,SOLUSDT
   PRIMARY_SYMBOL=BTCUSDT
   ```

2. Перезапустите веб-сервер (если он уже запущен)

3. Откройте вкладку "Symbols" в админке

4. **Проверьте:**
   - ✅ Все три символа отображаются в списке
   - ✅ Можно выбрать несколько символов чекбоксами
   - ✅ Можно установить PRIMARY_SYMBOL
   - ✅ Кнопка "Save Symbol Settings" работает
   - ✅ После сохранения настройки применяются

5. **Тестирование через API:**
   ```bash
   # Установить активные символы
   curl -X POST http://localhost:5000/api/symbols/set-active \
     -H "Content-Type: application/json" \
     -d '{"symbols": ["BTCUSDT", "ETHUSDT"], "primary": "BTCUSDT"}'
   ```

6. **Проверьте статус всех символов:**
   - Откройте `/api/status` и убедитесь, что `symbols_status` содержит информацию по всем активным символам

7. **Запустите бота:**
   - Нажмите кнопку "Start Bot" в Dashboard
   - Проверьте логи - должны запуститься воркеры для каждого активного символа
   - Проверьте, что в логах есть префикс `[symbol]` для каждого символа

---

### Сценарий 3: Переключение основного символа

**Цель:** Убедиться, что переключение основного символа работает корректно.

**Шаги:**
1. Установите активные символы: `BTCUSDT, ETHUSDT, SOLUSDT`

2. **Через UI:**
   - Используйте переключатель символов в верхней части страницы
   - Выберите ETHUSDT
   - Проверьте, что график обновился для ETHUSDT
   - Проверьте, что статус обновился для ETHUSDT

3. **Через вкладку Symbols:**
   - Откройте вкладку "Symbols"
   - Измените "Primary Symbol" на SOLUSDT
   - Сохраните настройки
   - Проверьте, что переключатель в верхней части обновился

4. **Через API:**
   ```bash
   curl -X POST http://localhost:5000/api/symbols/set-active \
     -H "Content-Type: application/json" \
     -d '{"symbols": ["BTCUSDT", "ETHUSDT", "SOLUSDT"], "primary": "SOLUSDT"}'
   ```

---

### Сценарий 4: График для разных символов

**Цель:** Убедиться, что график корректно отображается для каждого символа.

**Шаги:**
1. Для каждого символа (BTCUSDT, ETHUSDT, SOLUSDT):

   a. Выберите символ в переключателе
   
   b. Проверьте, что:
      - ✅ График загружается (не показывает ошибку)
      - ✅ Свечи отображаются
      - ✅ Индикаторы отображаются (SMA, BB, RSI, Volume, MACD)
      - ✅ Позиция (если есть) отображается правильно
      - ✅ Сигналы отображаются для этого символа

2. **Проверьте API:**
   ```bash
   # BTCUSDT
   curl http://localhost:5000/api/chart/data?symbol=BTCUSDT | jq '.symbol'
   
   # ETHUSDT
   curl http://localhost:5000/api/chart/data?symbol=ETHUSDT | jq '.symbol'
   
   # SOLUSDT
   curl http://localhost:5000/api/chart/data?symbol=SOLUSDT | jq '.symbol'
   ```
   
   Должно вернуться соответствующее значение `symbol`.

---

### Сценарий 5: Фильтрация данных по символу

**Цель:** Убедиться, что таблицы (Trades, Signals) фильтруются по символу.

**Шаги:**
1. Откройте вкладку "Trades"
2. Выберите символ в переключателе (например, ETHUSDT)
3. Проверьте, что таблица показывает только сделки для ETHUSDT
4. Переключите на другой символ - таблица должна обновиться

5. Откройте вкладку "Signals"
6. Выберите символ в переключателе
7. Проверьте, что таблица показывает только сигналы для выбранного символа

---

### Сценарий 6: Одновременная работа нескольких воркеров

**Цель:** Убедиться, что несколько воркеров работают одновременно без конфликтов.

**Шаги:**
1. Установите активные символы: `BTCUSDT,ETHUSDT,SOLUSDT`

2. Запустите бота:
   ```bash
   # Через API
   curl -X POST http://localhost:5000/api/bot/start
   ```

3. **Проверьте логи:**
   - Должны быть логи для каждого символа с префиксом `[BTCUSDT]`, `[ETHUSDT]`, `[SOLUSDT]`
   - Каждый воркер должен работать в своем потоке
   - Не должно быть конфликтов или блокировок

4. **Проверьте статус:**
   - Откройте `/api/status`
   - В `symbols_status` должна быть информация по каждому символу
   - Каждый символ должен показывать свой статус (`is_running`, `position`, и т.д.)

5. **Проверьте изоляцию:**
   - Каждый символ должен иметь свой `processed_signals_{symbol}.json` файл
   - Сделки для разных символов не должны смешиваться
   - Позиции для разных символов отслеживаются отдельно

---

## Проверка логов

### Что искать в логах:

1. **При запуске:**
   ```
   [web] MultiSymbolManager initialized with active symbols: ['BTCUSDT', 'ETHUSDT', 'SOLUSDT']
   [MultiSymbol] ✅ Started worker thread for BTCUSDT
   [MultiSymbol] ✅ Started worker thread for ETHUSDT
   [MultiSymbol] ✅ Started worker thread for SOLUSDT
   [live] [BTCUSDT] Starting live trading bot
   [live] [ETHUSDT] Starting live trading bot
   [live] [SOLUSDT] Starting live trading bot
   ```

2. **При работе:**
   ```
   [live] [BTCUSDT] 🔍 Checking for existing open positions...
   [live] [ETHUSDT] 📊 TREND strategy: generated X signals
   [live] [SOLUSDT] ✅ LONG position opened
   ```

3. **При переключении символов:**
   ```
   [web] Active symbols updated: ['BTCUSDT', 'ETHUSDT'], primary: BTCUSDT
   [MultiSymbol] ✅ Settings updated, active symbols: ['BTCUSDT', 'ETHUSDT']
   ```

---

## Возможные проблемы и решения

### Проблема 1: "MultiSymbolManager not initialized"

**Решение:**
- Убедитесь, что `init_app()` вызывается при запуске веб-сервера
- Проверьте логи при запуске - должно быть сообщение об инициализации

### Проблема 2: "Worker for {symbol} not found"

**Решение:**
- Убедитесь, что символ в списке `active_symbols`
- Проверьте, что символ валидный (BTCUSDT, ETHUSDT, SOLUSDT)
- Перезапустите веб-сервер после изменения настроек

### Проблема 3: График не обновляется при смене символа

**Решение:**
- Проверьте, что `loadChart()` передает параметр `symbol` в API
- Проверьте консоль браузера на наличие ошибок JavaScript
- Убедитесь, что API endpoint `/api/chart/data?symbol=...` работает

### Проблема 4: Настройки символов не сохраняются

**Решение:**
- Проверьте права на запись в `.env` файл
- Проверьте логи - должно быть сообщение `[web] Symbol settings saved to .env`
- Убедитесь, что функция `_save_symbol_settings_to_env()` вызывается

---

## Чек-лист успешного тестирования

- [ ] Конфигурация загружается без ошибок
- [ ] MultiSymbolManager инициализируется правильно
- [ ] Веб-интерфейс открывается и работает
- [ ] Вкладка "Symbols" отображается и работает
- [ ] Переключатель символов в верхней части работает
- [ ] API endpoints для символов работают
- [ ] График загружается для каждого символа
- [ ] Бот запускается для всех активных символов
- [ ] Логи показывают работу всех воркеров
- [ ] Настройки символов сохраняются в .env
- [ ] Переключение основного символа работает
- [ ] Таблицы фильтруются по символу (если реализовано)

---

## Следующие шаги

После успешного тестирования:

1. ✅ Можно использовать бота с несколькими торговыми парами
2. ✅ Можно переключать основной символ для отображения
3. ✅ Можно добавлять/удалять активные символы через UI

**Опционально:**
- Реализовать фильтрацию таблиц по символу
- Добавить сводную статистику по всем парам в Dashboard
- Улучшить детализацию статуса воркеров
